# 5C.7B — ARQUITECTURA EJECUTABLE DEL RUNTIME

## Estado

- Fecha: 28/07/2026.
- Alcance: 5C.7B.1 cloud-ready publicada y 5C.7B.2 cerrada y publicada; sin despliegue ni migración.
- Base: 5C.7B.1 publicada en `a8619c1`.
- Avance posterior publicado: G0002.5B.2E cerrada en `e4c79ff`, con commit atómico
  Confirmation → Mission y 179/179 pruebas aprobadas.
- G0002.5B.2F está cerrada arquitectónicamente; sus Fases 1.1, 1.2, 1.3 y 1.4 están
  aprobadas y cerradas en `G0002.5B.2F-ARQUITECTURA-PRODUCTIVA-POSTGRESQL-CLIENTE-CERO.md`.
- 5C.7B.3 está abierta; 5C.7B.3A queda aprobada y cerrada mediante
  `5C.7B.3A-CONTRATO-SECRETOS-MATRIZ-CUSTODIA.md`; 5C.7B.3B está cerrada y publicada
  en `4a5076c`; 5C.7B.3C está cerrada, con 3C.1 y 3C.2 cerradas.
  5C.7B.3D permanece abierta como contenedor, sin plan de pago ni gasto; 3D.1 está
  cerrada, con reversibilidad del proyecto demostrada documentalmente sin borrado
  real; 3D.2 queda **CERRADA**, con roles, migraciones 001/002 y verificación de
  metadatos ejecutados y verificados 33/33 en transacción de solo lectura; 3D.3
  queda **CERRADA**, con T1–T5 ejecutadas y superadas y TLS estricto demostrado
  —incluido `SCRAM-SHA-256-PLUS`— mediante sonda, no todavía en runtime productivo;
  3D.4 queda **CERRADA** (15/08/2026): su puerta humana de ejecución fue concedida,
  utilizada y **consumida** para las Puertas A y B de PG-RUN, y no habilita ninguna
  acción adicional; 3D.5 permanece cerrada/no abierta; **3D.6 queda abierta solo en
  planificación documental**, sin autorización para conectar a Neon ni escribir;
  5C.7B.3E–F continúan cerradas.
- Dentro de 3D.4, **PG-RUN = endpoint POOLED quedó CONFIRMADO empíricamente**
  (15/08/2026): el endpoint pooled real negoció `SCRAM-SHA-256-PLUS` con TLS estricto.
  Ese mismo día **PG-RUN quedó MATERIALIZADO** en Google Cloud Secret Manager
  (`OXKIO_MISSION_PG_RUNTIME_URL`, Version 1 habilitada, replicación automática).
  **Ningún consumidor productivo lo lee todavía**: el cierre de 3D.4 acredita custodia y
  acceso, no consumo.
- 5C.7B.3C adopta un proyecto Google Cloud dedicado para OXKIO, separado del proyecto
  Firebase actual, y Secret Manager con replicación global automática. Prevé identidades
  separadas de runtime, migración/administración y backup, sin claves descargadas y con
  IAM mínimo por secreto.
- 3C.1 creó `oxkio-runtime-prod`, vinculó billing y activó Secret Manager API. Las APIs base
  habilitadas automáticamente permanecen sin recursos asociados y no requieren actuación.
- 3C.2 creó las tres service accounts sin claves ni roles de proyecto. Service Account
  Credentials API quedó activa para la impersonación sin claves. El canario sintético
  demostró runtime permitido y migration/backup denegados; después se revocaron los bindings,
  se destruyó y eliminó el canario, quedando cero secretos operativos, cero bindings temporales
  y coste atribuible USD 0. La propagación IAM requirió un único reintento sin ampliar permisos.
- Aclaración explícita para 3D: las tres service accounts de 3C.2 (runtime, migración y
  backup) permanecen existentes y se reutilizarán sin crear identidades nuevas; lo
  eliminado fue exclusivamente el canario sintético de prueba y sus bindings temporales.
- 5C.7B.3C queda cerrada con mínimo privilegio y reversibilidad demostrados. PostgreSQL,
  TLS, RLS, roles y backups se transfieren a 3D; OAuth y tokens reales a 3E; Cloud Run,
  RPO/RTO, Owner humano e higiene de APIs automáticas a fases posteriores.
- Su guardarraíl IAM/Secret Manager es USD 0–0,20/mes; una previsión >= USD 1/mes exige
  revisión humana y nunca habilita gasto o ampliación automática. PostgreSQL real espera
  a 5C.7B.3D y OAuth real a 5C.7B.3E; Cloud Run y despliegue siguen fuera de alcance.
- Runtime candidato pendiente de auditoría: Cloud Run + Firebase Authentication.
- Persistencia operativa principal ratificada: PostgreSQL gestionado.
- Proveedor PostgreSQL seleccionado arquitectónicamente: Neon Launch; Cloud SQL
  Enterprise queda como contingencia. Runtime definitivo y contratación: pendientes.
- Veredicto 5C.7B.2: **PERSISTENCIA PRINCIPAL RATIFICADA** mediante
  `ADR-5C.7B.2-POSTGRESQL-PERSISTENCIA-PRINCIPAL.md`.

## Subfases de 5C.7B.3D (planificación)

| Subfase | Objetivo | Estado |
|---|---|---|
| 3D.1 | Neon: cuenta/proyecto Free creado (org. XANTALAL, proyecto OXKIO, Frankfurt, PostgreSQL 18); pooled/direct confirmados visualmente; sin tarjeta, plan de pago ni gasto; Neon Auth desactivado; reversibilidad del proyecto demostrada documentalmente | CERRADA |
| 3D.2 | Crear/configurar `oxkio_mission_owner` y `oxkio_mission_runtime` vía SQL controlado; aplicar 001/002 cuando exista autorización separada; verificación mínima de metadatos (propietario, atributos de rol, privilegios, RLS, políticas), sin escribir filas | **CERRADA** |
| 3D.3 | TLS/SSL: definir y demostrar la verificación estricta exigida por Neon en el cliente Node | **CERRADA** |
| 3D.4 | Secretos PostgreSQL reales en Secret Manager, ligados a las tres service accounts existentes de 3C.2 | **CERRADA** (15/08/2026); PG-RUN materializado con Version 1 habilitada, replicación automática y binding de acceso a nivel del secreto, bajo puerta humana ya consumida; sin consumidor productivo |
| 3D.5 | Backup (`pg_dump`, destino, retención), incluido el diseño y creación del rol de backup (trasladado desde 3D.2) | Cerrada / no abierta |
| 3D.6 | Pruebas/rollback contra instancia real | **ABIERTA solo en planificación documental** (15/08/2026); sin autorización para conectar a Neon, ejecutar SQL ni escribir |

### Hallazgo de seguridad — creación de roles de 3D.2 (11/08/2026)

Según documentación oficial de Neon consultada en esta fecha, los roles creados
mediante mecanismos gestionados de Neon (consola, CLI o API) reciben membresía
automática en `neon_superuser`, que otorga `CREATEDB`, `CREATEROLE`,
`BYPASSRLS` y `REPLICATION` — incompatible con el mínimo privilegio que exige
OXKIO. Por tanto: `oxkio_mission_owner` y `oxkio_mission_runtime` deberán
crearse exclusivamente mediante `CREATE ROLE` por SQL controlado, nunca
mediante el botón "New Role" de la consola Neon; `oxkio_mission_runtime`
deberá crearse con `LOGIN`, `NOINHERIT`, `NOSUPERUSER`, `NOCREATEDB`,
`NOCREATEROLE` y `NOBYPASSRLS`, y no será miembro de `oxkio_mission_owner`.
No se ha creado ningún rol real. Fuente: Neon Docs — Manage roles
(`neon.com/docs/manage/roles`).

### Frontera 3D.2 / 3D.6 — alcance de la verificación (11/08/2026)

3D.2 permite únicamente la futura verificación de catálogo/metadatos
necesaria para comprobar propietarios, atributos de roles, privilegios, RLS
`ENABLE`/`FORCE` y políticas — sin `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE` ni
escritura de filas. Las pruebas que impliquen escritura/truncado de datos
sintéticos, CAS, concurrencia, rollback/timeout, restore, portabilidad o
validación integral permanecen en 3D.6, que sigue cerrada/no abierta. No se
autoriza ni ejecuta ningún test en esta apertura.

### Procedimiento futuro de credenciales para 3D.2 (gobernanza, sin valores reales)

Cualquier credencial real requerirá autorización humana separada. Cuando
llegue ese momento, José Antonio introducirá los valores directamente en su
terminal local; nunca se pegarán en el chat, nunca se registrarán en
documentación, nunca se guardarán en el repositorio, no se creará un `.env`
nuevo con esos valores, y no se imprimirán. Se usarán únicamente variables de
entorno temporales durante la sesión y se retirarán del entorno al terminar.
La contraseña de `oxkio_mission_runtime` no existe todavía; su generación
sigue pendiente de la futura ejecución autorizada.

### Evidencia consolidada de cierre de 3D.1 (11/08/2026)

Valores observados en la consola Neon en esta fecha; evidencia puntual y variable
de este proyecto concreto, no constantes arquitectónicas permanentes.

- Endpoint pooled (sufijo `-pooler`, con "Agrupación de conexiones" activada) y
  endpoint direct (sin `-pooler`, con esa opción desactivada) confirmados
  visualmente, sin revelar contraseñas ni copiar connection strings completas.
- La consola muestra `sslmode=require` por defecto; esto no cerraba 3D.3, que
  entonces seguía pendiente para demostrar `verify-full`/`channel_binding` desde el
  cliente Node.
- Reversibilidad del proyecto demostrada documentalmente mediante fuentes
  oficiales Neon, sin ejecutar borrado real: un Admin del proyecto puede
  eliminarlo; el borrado elimina computes, branches, databases y roles del
  proyecto; existe recuperación durante 7 días vía API/CLI; tras esa ventana
  el borrado es permanente. La reversibilidad exigida es la del proyecto
  `OXKIO`, no de la organización `XANTALAL` ni de la cuenta. Fuentes: Neon
  Docs — Manage projects (`neon.com/docs/manage/projects`), Neon API —
  Recover a deleted project (`api-docs.neon.tech/reference/recoverproject`)
  y Neon Docs — Manage Organizations (`neon.com/docs/manage/orgs-manage`).

Con esta evidencia, 3D.1 queda cerrada. Se mantiene la arquitectura ya aprobada
de runtime mediante endpoint pooled y administración/migración mediante
endpoint directo. El cierre de 3D.1 no autoriza migraciones, roles reales, RLS,
secretos, TLS productivo, suite de pruebas, datos reales, OAuth, Firebase,
Cloud Run ni ningún plan de pago, y no abre automáticamente 3D.2.

### Evidencia consolidada de cierre de 3D.2 (13/08/2026)

Ejecución real autorizada mediante puerta humana separada, concedida y consumida
para esta subfase. Fases superadas: `preflight`, `roles`, `password`, `apply-001`,
`apply-002` y `verify` final **33/33** en transacción `BEGIN READ ONLY`,
catalog-only. No se insertó ninguna fila, real ni sintética. Las credenciales
temporales se retiraron del entorno al terminar.

Estado alcanzado en Neon: roles `oxkio_mission_owner` y `oxkio_mission_runtime`;
esquema `oxkio`; tablas `oxkio.missions` y `oxkio.mission_confirmations`; RLS
`ENABLE` + `FORCE`; políticas de aislamiento por `tenant_id` + `user_id` +
`client_id`; 10 índices; 22 CHECK; 56 constraints en total.

Privilegios verificados del runtime: `SELECT` + `INSERT` en ambas tablas; `UPDATE`
únicamente sobre 13 columnas exactas de `missions` y 8 de `mission_confirmations`,
comparadas por nombre contra el `GRANT` de cada migración; `DELETE`, `TRUNCATE` y
`UPDATE` a nivel de tabla denegados. `oxkio_mission_runtime` sin `SUPERUSER`,
`BYPASSRLS`, `CREATEDB`, `CREATEROLE`, sin membresía en `oxkio_mission_owner` y
sin `neon_superuser`.

Hallazgos registrados:

1. Los roles OXKIO se crearon mediante SQL controlado, nunca por consola, CLI o API
   gestionada de Neon, conforme al hallazgo de seguridad del 11/08/2026. Se confirma
   la ausencia de `neon_superuser`.
2. Neon no admite en este flujo un verificador SCRAM-SHA-256 precomputado como
   `PASSWORD`: su control plane exige la contraseña en claro y rechaza el delta, lo
   que abortaba la transacción con `XX000` / `SendDeltasToControlPlane`. La fase
   `password` se corrigió al mecanismo compatible, manteniendo la contraseña fuera
   del chat, del repositorio y del disco.
3. El primer `verify` dio 28/31 por falsos negativos de
   `information_schema.table_privileges` y `column_privileges`: esas vistas solo
   muestran filas cuyo grantor o grantee sea un rol actualmente habilitado, y el
   diseño deliberado `INHERIT FALSE` lo impide. Se corrigió **únicamente el
   verificador**, que pasó a leer la ACL directa (`pg_class.relacl`,
   `pg_attribute.attacl`) contrastada con `has_*_privilege`. No se modificó ningún
   permiso real. `verify` final: 33/33.
4. Límite explícito: esto demuestra que **RLS está CONFIGURADO** correctamente. **No
   demuestra aislamiento funcional** entre dos scopes mediante escritura de filas.
   Esa validación pertenece a 3D.6, junto con CAS, concurrencia, rollback, restore
   y portabilidad.

Herramienta de evidencia y rollback: runner efímero en
`C:\Users\janta\AppData\Local\OXKIO\tools\oxkio-3d2-apply.js`, sha256
`506fbdfa9acf20b7d38175fb0f04aaef191d3330547ea382b963bd58e44af1a9`, selftest
offline 26/26. Está fuera del repositorio, de OneDrive y de Temp, sin secretos
embebidos. Se conserva al menos hasta el cierre de 3D.6 por ser el portador del
SQL de rollback y el reproductor de la evidencia; **no se versiona en Git**.

Con esta evidencia, 3D.2 queda cerrada. El cierre no autoriza TLS productivo,
secretos reales en Secret Manager, rol de backup, pruebas con escritura, datos
reales, OAuth, Firebase, Cloud Run ni ningún plan de pago, y **no abre
automáticamente 3D.3**.

### Apertura controlada de 5C.7B.3D.3 — política TLS/SSL (13/08/2026)

En su apertura, 3D.3 quedó **abierta exclusivamente en modo controlado de
planificación/preparación**. Aquella apertura **no autorizaba** conexión real a Neon,
credenciales, ejecución de las pruebas T1–T5, cambios de código productivo, Secret
Manager, backup, `server.js`, escritura de datos ni 3D.4–3D.6.

**Política TLS/SSL exigida por OXKIO** (redactada en la apertura; su demostración
consta en la evidencia de cierre más abajo):

1. TLS obligatorio en toda conexión PostgreSQL.
2. CA válida y verificada contra un almacén de confianza.
3. Hostname del servidor verificado.
4. SNI correcto y explícito.
5. `rejectUnauthorized = true` siempre.
6. No depender de la semántica de `sslmode=require`.
7. No pasar la `connectionString` completa a `pg` cuando pueda sobrescribir la
   política TLS: los campos se descomponen y se fijan explícitamente.
8. `enableChannelBinding = true`.
9. **FAIL CLOSED** si no se demuestra la negociación de `SCRAM-SHA-256-PLUS`.

**Evidencia reutilizable procedente de 3D.2** (no cierra 3D.3): el runner de 3D.2
conecta con `rejectUnauthorized: true`, `servername`/SNI explícito, contra el
endpoint **directo**, y descompone la URL en campos en lugar de reenviarla. Con
ello, la verificación de **CA y hostname queda cubierta en sustancia**. Esta
evidencia **NO cierra 3D.3** y no demuestra channel binding.

**Constataciones técnicas registradas** (entorno real: Node 24, `pg@8.22.0`,
PostgreSQL 18, Neon):

- `pg@8.22.0` **soporta** channel binding (`SCRAM-SHA-256-PLUS`, `tls-server-end-point`).
- Ese soporte **no está activado** en el runner de 3D.2: la opción es opcional y su
  valor por defecto es desactivado, de modo que el runner de 3D.2 negocia sin channel
  binding. La demostración de `SCRAM-SHA-256-PLUS` se obtuvo después, en T4, con la
  sonda de 3D.3; sigue **sin demostrarse en el runtime productivo**.
- Depender de la semántica actual de `sslmode=require` es un riesgo: el propio
  paquete advierte de que ese tratamiento cambiará en versiones futuras. La política
  de OXKIO debe ser **explícita y estable** frente a cambios de versión, y no
  apoyarse en valores por defecto de la librería.
- **No existe todavía política TLS en el código productivo de OXKIO.**

**Plan de prueba T1–T5** (redactado en la apertura; **ejecutado y superado**
posteriormente — resultados en la evidencia de cierre):

| Prueba | Objetivo | Resultado exigido |
|---|---|---|
| T1 | Conexión positiva al endpoint directo | TLS, CA y hostname correctos |
| T2 | Hostname incorrecto | **FAIL CLOSED** |
| T3 | CA incorrecta | **FAIL CLOSED** |
| T4 | `enableChannelBinding = true` | Demostrar `SCRAM-SHA-256-PLUS`; si no se negocia PLUS, **FAIL CLOSED** |
| T5 | Anti-downgrade, **OFFLINE** | La política evita que la cadena sobrescriba las opciones TLS |

Cada prueba se ejecutó bajo puerta humana separada; T1 y T4 con credencial de solo
conexión del rol runtime, T2 y T3 sin credencial alguna, y T5 offline.

**Frontera**: 3D.3 cubre TLS, CA, hostname, SNI, channel binding y anti-downgrade.
Secret Manager pertenece a 3D.4; backup y rol de backup a 3D.5; escritura,
aislamiento funcional entre scopes, CAS, concurrencia, rollback, restore y
portabilidad a 3D.6.

### Evidencia consolidada de cierre de 5C.7B.3D.3 (13/08/2026)

Las cinco pruebas se ejecutaron con la sonda efímera `oxkio-3d3-tls-probe.js` y
**todas resultaron PASS**:

| Prueba | Resultado | Evidencia |
|---|---|---|
| T1 | **PASS** | TLS autorizado; `authorized=true`; `authorizationError=null`; certificado vigente; SAN presente; `SELECT 1` correcto |
| T2 | **PASS** | Hostname falso rechazado; `authorized=false`; `ERR_TLS_CERT_ALTNAME_INVALID`; sin credenciales; sin SQL |
| T3 | **PASS** | CA no confiable rechazada; `authorized=false`; `UNABLE_TO_GET_ISSUER_CERT_LOCALLY`; sin credenciales; sin SQL |
| T4 | **PASS** | `enableChannelBinding=true`; observador invocado; mecanismo negociado **`SCRAM-SHA-256-PLUS`**; conexión establecida |
| T5 | **PASS** (offline) | Riesgo de `connectionString` reproducido; patrón seguro de OXKIO validado |

**TLS estricto demostrado**: verificación de CA, verificación de hostname, SNI
correcto, `rejectUnauthorized=true` y certificado vigente, con **fallo cerrado**
tanto ante un hostname incorrecto como ante una CA incorrecta. El valor probatorio
está en los pares: T1 sola no bastaría, son T2 y T3 las que demuestran que la
identidad y la cadena se comprueban de verdad.

**Channel binding demostrado positivamente**: T4 no se conforma con afirmar
`enableChannelBinding=true`; observa el mecanismo realmente enviado durante el
handshake y exige que sea `SCRAM-SHA-256-PLUS`, fallando cerrado en cualquier otro
caso. El resultado fue `SCRAM-SHA-256-PLUS` con conexión establecida.

**Anti-downgrade**: se reprodujo offline que una `connectionString` puede alterar la
política TLS explícita, y se validó el patrón seguro de OXKIO —descomponer la URL y
fijar `host`, `port`, `user`, `password`, `database`, `ssl` y `enableChannelBinding`
de forma explícita—, que preserva la política intacta.

**Comportamiento verificado, no vulnerabilidad**: `pg` sobrescribe `ssl.servername`
con el `host` de la conexión (`connection.js::upgradeToSSL`). El SNI queda por tanto
atado al host, lo que impide tanto fijarlo a mano como debilitarlo por descuido. Se
registra como constatación técnica del cliente, no como defecto.

**Distinción imprescindible**:

- **DEMOSTRADO**: la política TLS ejercida **mediante la sonda**.
- **NO DEMOSTRADO NI IMPLEMENTADO**: el **cableado de esa política en el runtime
  productivo / `server.js`**. El código productivo sigue sin configuración TLS y los
  repositorios reciben el pool inyectado.

**PENDIENTE TRANSVERSAL DE RUNTIME/COMPOSICIÓN**: cablear la política TLS
demostrada en la raíz de composición del runtime productivo. Es **obligatorio antes
de cualquier conexión o despliegue productivo**. Su propietario y su fase exacta se
decidirán al preparar 3D.4 o en una puerta posterior; **este cierre no lo asigna a
ninguna subfase y no amplía el alcance canónico de 3D.4**, que sigue siendo
exclusivamente los secretos PostgreSQL reales en Secret Manager.

**Herramienta de evidencia**: `C:\Users\janta\AppData\Local\OXKIO\tools\oxkio-3d3-tls-probe.js`,
sha256 `0185de026abff7f73e13e3fcbbca371e810f6a5d370c330aee15379e311f8bcc`, selftest
offline 20/20, hash verificado sin cambios entre la auditoría y la ejecución. Fuera
del repositorio, de OneDrive y de Temp, sin secretos embebidos. Se conserva al menos
hasta el cierre de 3D.6 —será necesaria para revalidar tras cualquier subida de `pg`,
en especial la que invierta la semántica de `sslmode`— y **no se versiona en Git**.
Las credenciales temporales se eliminaron del entorno al terminar.

Con esta evidencia, 3D.3 queda cerrada. El cierre no autoriza Secret Manager, rol de
backup, pruebas con escritura, datos reales, cambios en código productivo,
`server.js`, despliegue, OAuth, Firebase, Cloud Run ni ningún plan de pago, y **no
abre automáticamente 3D.4**.

### Apertura controlada de 5C.7B.3D.4 — Secret Manager (13/08/2026)

3D.4 queda **abierta exclusivamente en modo controlado de planificación/
preparación**. Esta apertura **no autoriza** `gcloud`, Google Cloud, creación de
secretos o versiones, cambios de IAM, Secret Manager real, credenciales, Neon, SQL,
`server.js`, código productivo, TLS productivo, backup, ni 3D.5–3D.6 ni 3E–F.

**Alcance canónico — sin cambios**: «Secretos PostgreSQL reales en Secret Manager,
ligados a las tres service accounts existentes de 3C.2». Proyecto ya existente:
`oxkio-runtime-prod`, con Secret Manager API activada en 3C.1.

**Decisiones de planificación registradas:**

1. **Primera tarea — RESUELTA el 14/08/2026**: inventariar en **solo lectura** las
   tres service accounts de 3C.2 (runtime, migración y backup) para recuperar sus
   IDs exactos. Inventario registrado más abajo en «Inventario canónico de service
   accounts (3C.2)»; no quedan identidades desconocidas.
2. **Secreto inicial**: únicamente `OXKIO_MISSION_PG_RUNTIME_URL` (**PG-RUN**).
3. **PG-MIG no se materializa ahora**: `OXKIO_MISSION_PG_ADMIN_URL` queda reservado
   para operaciones y migraciones controladas, conforme al contrato 5C.7B.3A, que
   exige «evitar URL administrativa permanente».
4. **PG-BKP no se materializa**: pertenece a 3D.5.
5. **Formato de PG-RUN**: URL **sin parámetros de consulta**, en particular **sin
   `sslmode`**; queda **prohibido** pasarla directamente a `pg` como
   `connectionString`; el consumidor futuro deberá parsearla y reconstruir los
   campos, imponiendo **TLS estricto y channel binding por código**, conforme a lo
   demostrado en 3D.3.
6. **IAM previsto**: `roles/secretmanager.secretAccessor` **solo** para la service
   account de runtime y **solo sobre PG-RUN**, vinculado a nivel del secreto y
   nunca a nivel de proyecto.
7. El **PENDIENTE TRANSVERSAL DE RUNTIME/COMPOSICIÓN** —cablear realmente TLS en el
   runtime productivo/`server.js`— **se mantiene** y **no se adjudica a 3D.4**.

### Inventario canónico de service accounts (3C.2) — verificado el 14/08/2026

Identidades existentes en el proyecto `oxkio-runtime-prod`, verificadas
manualmente por el operador humano en Google Cloud Console. Son las **mismas tres
creadas en 3C.2**: no se crean identidades nuevas.

| Rol | Display name | Email / identidad | Estado | Claves de usuario |
|---|---|---|---|---|
| Runtime | OXKIO Runtime Production | `oxkio-runtime-prod@oxkio-runtime-prod.iam.gserviceaccount.com` | Habilitada | ninguna |
| Migración | OXKIO Migration Production | `oxkio-migration-prod@oxkio-runtime-prod.iam.gserviceaccount.com` | Habilitada | ninguna |
| Backup | OXKIO Backup Production | `oxkio-backup-prod@oxkio-runtime-prod.iam.gserviceaccount.com` | Habilitada | ninguna |

No se observaron cuentas adicionales ni duplicadas. La ausencia de claves
administradas por usuario es coherente con 3C.2, que creó las tres identidades
**sin claves descargadas y sin roles de proyecto**, con impersonación vía Service
Account Credentials API.

**Aviso de legibilidad**: el account ID de runtime (`oxkio-runtime-prod`) coincide
literalmente con el project ID (`oxkio-runtime-prod`), de modo que su email repite
el término a ambos lados de la `@`. No es un error ni una colisión —el espacio de
nombres de service accounts es independiente del de proyectos— pero obliga a
extremar la precisión al redactar futuros bindings IAM, donde confundir identidad
con proyecto produciría exactamente el binding a nivel de proyecto que 3D.4
prohíbe.

Estas identidades son identificadores, **no credenciales**: no otorgan acceso por
sí solas y su registro documental no concede permiso alguno.

**Pendientes registrados en esta apertura:**

- ~~Los **IDs exactos de las tres service accounts no constan** en ningún documento
  canónico; sin ellos no puede vincularse IAM.~~ **RESUELTO el 14/08/2026**: ver
  «Inventario canónico de service accounts (3C.2)». Resolver este pendiente **no
  abre** la ejecución real de 3D.4 ni autoriza binding IAM alguno.
- `OXKIO_MISSION_PG_RUNTIME_URL` sigue declarada **`optional`** en
  `backend/config/environment-contract.js`.
- Antes de cualquier runtime productivo deberá existir **fallo cerrado** si el
  secreto falta, es inválido o ha sido revocado, conforme al punto 8 del contrato
  5C.7B.3A.

### Evidencia empírica — TP1 POOLED / channel binding (15/08/2026)

Medición única, sin repetición ni reintentos, con la sonda endurecida
`oxkio-3d4-pooler-cb-probe.js` (sha256
`bef3f96ddd604d3781545b3a8dd18d25c68bdcd9c9b2a68b51b2e9f24224828b`, selftest offline
**47/47**). Resultado **PASS**.

| Observación | Valor |
|---|---|
| Endpoint medido | **POOLED** (sufijo `-pooler` exigido por la propia sonda) |
| Identidad | `oxkio_mission_runtime`, exigida en exclusiva antes de abrir socket |
| Cliente | `pg` 8.22.0 |
| TLS | autorizado, `rejectUnauthorized=true` |
| `enableChannelBinding` | `true` |
| Mecanismo SASL negociado | **`SCRAM-SHA-256-PLUS`** |
| Conexión | completada |
| Comprobación | `SELECT 1` correcta |
| Reintentos | 0 |

**PG-RUN = POOLED queda CONFIRMADO.** No se propone cambio a DIRECT. La
contradicción documental que motivó la medición —Neon publica
`channel_binding=require` incluso en ejemplos con host `-pooler`, mientras que el
changelog de PgBouncer 1.12.0 declara no implementar channel binding— queda resuelta
a favor de la evidencia del proveedor real: la documentación genérica de PgBouncer
queda **subordinada** a lo medido contra el endpoint pooled real de Neon.

**Ausencia de secretos**: la salida mostró el hostname **enmascarado**. No se
registran aquí ni en ningún otro documento el hostname completo, la contraseña ni la
cadena de conexión. Tras la prueba se retiraron del entorno `OXKIO_POOLER_PG_URL`,
`OXKIO_REPO_ROOT` y todos los temporales de credencial.

**Lección técnica registrada — construcción de la URI**: una contraseña válida de
runtime puede contener caracteres reservados de URI. Al construir una URI de
PostgreSQL, **usuario y contraseña deben percent-encodearse antes de incorporarlos al
userinfo**; nunca se codifican protocolo, host, puerto, path ni separadores
estructurales. Un intento previo de TP1 con la contraseña interpolada en crudo falló
**localmente, antes de contactar con ningún servidor**, y quedó **inconcluso y sin
valor arquitectónico**. La sonda endurecida exige ahora una representación
percent-encoded inequívoca y distingue en su salida un fallo local de preparación de
un fallo real de Neon/TLS/SASL. No se registra la contraseña ni qué carácter concreto
contiene.

**Herramienta de evidencia**:
`C:\Users\janta\AppData\Local\OXKIO\tools\oxkio-3d4-pooler-cb-probe.js`, fuera del
repositorio, de OneDrive y de Temp, sin secretos embebidos y **no versionada en Git**;
se conserva al menos hasta el cierre de 3D.6.

Esta evidencia **no autorizaba por sí sola** crear el secreto ni su versión en Secret
Manager, ni ningún binding IAM. Esa autorización llegó después, como puerta humana
separada, y su ejecución se registra en «Materialización real de PG-RUN — Puertas A y
B».

### Plan mínimo de materialización de PG-RUN — EJECUTADO el 15/08/2026

Secret ID: `OXKIO_MISSION_PG_RUNTIME_URL` (**PG-RUN**), ya ratificado en el contrato
5C.7B.3A y en el punto 2 de la apertura de 3D.4. Corresponde **exclusivamente** a
PG-RUN/runtime: no se mezcla con **PG-MIG** (`OXKIO_MISSION_PG_ADMIN_URL`, reservado,
no se materializa), con **PG-BKP** (3D.5) ni con ninguna identidad
owner/admin/bootstrap.

Pasos previstos, en este orden y cada uno bajo puerta humana. **Los cuatro se
ejecutaron manualmente en la consola web el 15/08/2026**; el resultado observado se
recoge en la sección siguiente:

1. Crear el secreto `OXKIO_MISSION_PG_RUNTIME_URL` en `oxkio-runtime-prod`, con la
   replicación automática ya adoptada en 3C y todavía sin ninguna versión.
2. Añadir la **primera versión** con la URL de runtime: esquema `postgresql://`,
   identidad `oxkio_mission_runtime`, **contraseña percent-encoded**, host **pooled**
   y base de datos, **sin parámetros de consulta y sin `sslmode`**.
3. Conceder `roles/secretmanager.secretAccessor` **solo** a
   `oxkio-runtime-prod@oxkio-runtime-prod.iam.gserviceaccount.com` y **solo sobre ese
   secreto**, a nivel del propio secreto y nunca a nivel de proyecto.
4. Verificar en solo lectura el binding resultante y la ausencia de cualquier otro
   principal con acceso.

Estado de los requisitos que este plan exigía antes del paso 1:

- **CUMPLIDO** — puerta humana explícita para la ejecución real de 3D.4: concedida el
  15/08/2026 por José Antonio y la dirección externa, utilizada para las Puertas A y B
  y **consumida**. No se extiende a ninguna otra acción, fase ni secreto.
- **PENDIENTE** — registrar y aplicar que el consumidor **percent-decodifica** el
  userinfo tras parsear la URL, coherente con la lección de encoding y con la
  prohibición de pasarla a `pg` como `connectionString`.
- **PENDIENTE** — `OXKIO_MISSION_PG_RUNTIME_URL` sigue declarada `optional` en
  `backend/config/environment-contract.js` y debe pasar a fallo cerrado.
- **PENDIENTE** — cablear TLS en la raíz de composición del runtime, obligatorio antes
  de cualquier conexión productiva y no adjudicado a 3D.4.

### Materialización real de PG-RUN — Puertas A y B (15/08/2026)

Ejecución **manual** en la consola web de Google Cloud por el operador humano, bajo
dos puertas separadas. OXKIO no ejecutó `gcloud`, no usó credenciales de aplicación,
no impersonó ninguna identidad y **no accedió en ningún momento al valor del secreto**.
Lo que sigue es, por tanto, **evidencia verificada por el operador en consola**, del
mismo tipo que el inventario de service accounts de 3C.2, y no una medición
automatizada.

**A. Puerta A — secreto y primera versión.** En el proyecto `oxkio-runtime-prod`
existe el secreto `OXKIO_MISSION_PG_RUNTIME_URL`. La pestaña de versiones muestra
**exactamente Version 1**, en estado **habilitada**, con **cifrado administrado por
Google**. No se creó ninguna segunda versión, no se destruyó ni deshabilitó ninguna, y
el payload no se visualizó después de crearlo. El valor corresponde a PG-RUN: rol
`oxkio_mission_runtime`, endpoint POOLED de Neon, contraseña percent-encoded dentro
del userinfo, sin parámetros de consulta y sin `sslmode`. Ni el valor, ni el host
completo, ni la contraseña se registran en ningún documento.

**B. Puerta B — acceso a nivel del secreto.** La pestaña de permisos del propio
secreto muestra el principal
`oxkio-runtime-prod@oxkio-runtime-prod.iam.gserviceaccount.com`
(«OXKIO Runtime Production») con el rol que la consola presenta como **«Usuario con
acceso a secretos de Secret Manager»**, sin condición IAM.

**Precisión deliberada sobre el identificador del rol**: la interfaz web localizada
**no mostró literalmente** el ID técnico. `roles/secretmanager.secretAccessor` es la
**correspondencia esperada** del rol predefinido observado, no un dato leído de esa
pantalla. Se registra así para no convertir una inferencia en una medición.

**Sujetos con acceso — enunciado honesto.** La vista de permisos del secreto mostró
dos entradas: la service account de runtime, con binding **explícito** sobre el propio
secreto, y `xantalal@gmail.com` como **Propietario heredado** del proyecto «OXKIO
Runtime». Por tanto **no** puede afirmarse que la identidad de runtime sea el único
sujeto capaz de leer el secreto; lo que sí puede afirmarse es que es el **único binding
explícito de acceso a secretos observado a nivel del propio secreto**, junto al acceso
administrativo heredado del propietario. Ese acceso heredado **no se modifica** aquí.
Las service accounts de migración y backup **no aparecen** en los permisos del secreto.

**IAM a nivel de proyecto.** En «IAM y administración → IAM» del proyecto, la vista
por entidades mostró únicamente `xantalal@gmail.com` como Propietario. No se
observaron bindings de Secret Manager —ni de ningún otro tipo a nivel de proyecto—
para las tres service accounts. Es una **observación de consola**, no una prueba
automatizada de inexistencia.

**Lo que esta materialización NO demuestra.** Sigue sin demostrarse que Cloud Run o el
runtime puedan leer realmente PG-RUN; que el runtime productivo consuma Secret
Manager; que parsee la URL o percent-decodifique el userinfo correctamente; que
conecte con Neon; que use TLS estricto o channel binding en producción; que
`environment-contract.js` falle cerrado; y que exista despliegue funcional. Ninguna de
esas capacidades puede darse por adquirida a partir de la existencia del secreto.

**Alcance.** PG-MIG (`OXKIO_MISSION_PG_ADMIN_URL`) y PG-BKP siguen **no
materializados**. 3D.5, 3D.6 y 5C.7B.3E–F siguen cerradas/no abiertas. La puerta humana
usada aquí queda **consumida**. Crear el secreto y conceder el acceso no cerraba por sí
solo la subfase: el cierre se produjo tras la auditoría formal recogida en la sección
siguiente.

### Evidencia consolidada de cierre de 5C.7B.3D.4 — 15/08/2026

**Alcance auditado.** El contrato de 3D.4, fijado en su apertura controlada del
13/08/2026, se compone de siete decisiones. La auditoría formal comprobó una a una su
satisfacción y clasificó cada pendiente restante según perteneciera a esta subfase o a
otra. El resultado: el alcance canónico está **satisfecho o correctamente transferido**.

| Criterio de 3D.4 | Estado | Evidencia |
|---|---|---|
| 1. Inventario read-only de las tres service accounts de 3C.2 | **CUMPLIDO** | Registrado el 14/08/2026 en «Inventario canónico de service accounts (3C.2)» |
| 2. PG-RUN como único secreto inicial | **CUMPLIDO** | Existe únicamente `OXKIO_MISSION_PG_RUNTIME_URL` |
| 3. PG-MIG no se materializa | **CUMPLIDO** | `OXKIO_MISSION_PG_ADMIN_URL` sigue reservado y sin materializar |
| 4. PG-BKP no se materializa (3D.5) | **CUMPLIDO** | Sin materializar |
| 5. Formato de PG-RUN y prohibición de `connectionString` | **CUMPLIDO** | Valor creado sin parámetros de consulta y sin `sslmode`; la obligación de TLS/channel binding por código corresponde, por el propio criterio, al consumidor futuro |
| 6. Mínimo privilegio a nivel del secreto | **CUMPLIDO** | Binding único y explícito para la service account de runtime sobre el propio secreto; sin bindings de proyecto observados para las tres identidades |
| 7. TLS productivo fuera del alcance de 3D.4 | **CUMPLIDO** | El pendiente transversal sigue sin adjudicarse a esta subfase |

**Evidencia técnica previa (medida por OXKIO).** TP1 contra el endpoint POOLED real,
resultado **PASS**: `pg` 8.22.0, TLS autorizado, `rejectUnauthorized=true`,
`enableChannelBinding=true`, mecanismo negociado **`SCRAM-SHA-256-PLUS`**, conexión
completada, `SELECT 1` correcto, cero reintentos, identidad `oxkio_mission_runtime`.
Sonda `oxkio-3d4-pooler-cb-probe.js`, sha256
`bef3f96ddd604d3781545b3a8dd18d25c68bdcd9c9b2a68b51b2e9f24224828b`, selftest offline
47/47.

**Evidencia de custodia (verificada por el operador en consola).** El secreto
`OXKIO_MISSION_PG_RUNTIME_URL` existe en `oxkio-runtime-prod` con **exactamente Version
1, habilitada**; política de replicación **«Replicado automáticamente»**, coherente con
la replicación global automática adoptada en 3C; cifrado **«Administrada por Google»**;
rotación **«Sin programar»**; vencimiento **«Nunca»**. El payload **no se recuperó, no
se mostró y no se registró** en ningún momento.

**Evidencia de acceso.** Sobre el propio secreto figura
`oxkio-runtime-prod@oxkio-runtime-prod.iam.gserviceaccount.com` («OXKIO Runtime
Production») con el rol que la consola muestra literalmente como **«Usuario con acceso
a secretos de Secret Manager»**, sin condición IAM. La interfaz web **no expone el
identificador técnico**: `roles/secretmanager.secretAccessor` se consigna como
**correspondencia esperada del rol predefinido observado**, nunca como una lectura
literal. Se decidió expresamente **no** instalar `gcloud`, no crear credenciales por
defecto, no impersonar identidades y no ampliar permisos únicamente para conocer ese
identificador: el criterio de cierre describe la evidencia realmente observada.

**Sujetos con acceso.** Junto al binding explícito de la service account de runtime
figura `xantalal@gmail.com` como **Propietario heredado** del proyecto. Por tanto la
identidad de runtime **no** es el único sujeto capaz de leer el secreto; sí es el único
binding explícito de acceso a secretos observado a nivel del propio secreto. Las
service accounts de migración y backup **no aparecen** en los permisos del secreto, y la
vista IAM del proyecto mostró únicamente al Propietario, sin bindings de proyecto para
las tres identidades. La retirada del Owner humano permanece transferida a fases
posteriores.

**Trazabilidad de las operaciones.** La pestaña de registros del secreto mostró tres
eventos y su secuencia: `CreateSecret` sobre `OXKIO_MISSION_PG_RUNTIME_URL`,
`AddSecretVersion` sobre `OXKIO_MISSION_PG_RUNTIME_URL:1` y `SetIamPolicy` sobre el
secreto. Creación → versión → IAM. Los registros no se expandieron y no se extrajo de
ellos ningún dato adicional.

**Puerta humana.** Concedida el 15/08/2026 para las Puertas A y B, utilizada y
**consumida**. No habilita ninguna acción adicional.

**Lo que este cierre NO afirma.** No se afirma que el runtime productivo consuma
PG-RUN, que Cloud Run lea el secreto, que el runtime parsee la URL o percent-decodifique
el userinfo, que conecte con Neon, que use TLS estricto o channel binding en
producción, que `environment-contract.js` falle cerrado, ni que exista despliegue
funcional.

**Pendientes transferidos, no ejecutados.** Al **consumidor productivo**: percent-decode
del userinfo tras parsear la URL. Al **contrato/runtime productivo**:
`OXKIO_MISSION_PG_RUNTIME_URL` sigue `optional` en
`backend/config/environment-contract.js`, y el fallo cerrado ante secreto ausente,
inválido o revocado que exige el punto 8 de 5C.7B.3A. Al **pendiente transversal de
runtime/composición**: cablear TLS y channel binding en la raíz de composición. A
**runtime/Cloud Run posterior**: el consumo real desde Secret Manager y su lectura
efectiva. A **3D.6 / pruebas reales**: la conexión runtime → Neon. A **fases
posteriores**: el despliegue funcional y la retirada del Owner humano. Ninguno de estos
pendientes pertenecía al contrato de 3D.4 y ninguno se ejecuta en este cierre.

**Con esta evidencia, 5C.7B.3D.4 queda CERRADA.** El cierre **no** autoriza nuevas
versiones del secreto, cambios de IAM, PG-MIG, PG-BKP, TLS productivo, cambios en
`environment-contract.js` o `server.js`, despliegue, rol de backup, pruebas con
escritura, datos reales, contratación ni gasto, y **no abre** 3D.5, 3D.6 ni 5C.7B.3E–F.
La acción siguiente fue **evaluar** qué subfase corresponde abrir; el resultado de esa
evaluación se recoge en la sección siguiente.

### Apertura controlada de 5C.7B.3D.6 — pruebas/rollback (15/08/2026)

3D.6 queda **abierta exclusivamente en modo de planificación documental**. Esta
apertura **no autoriza** conectar con Neon, ejecutar SQL, escribir o truncar filas,
usar credenciales reales, leer PG-RUN, tocar Secret Manager, IAM, `gcloud`, código
productivo, `server.js`, `environment-contract.js`, TLS de composición, despliegue,
PG-MIG, PG-BKP, `pg_dump` ni restore. La ejecución exige una **segunda puerta humana**
nueva y explícita; la de 3D.4 quedó consumida y no se reutiliza.

**Por qué 3D.6 antes que 3D.5.** El riesgo abierto de mayor impacto no es la pérdida de
datos —la base tiene el esquema de 001/002 aplicado y **cero filas productivas**— sino
que el aislamiento por RLS está **configurado pero nunca demostrado funcionalmente**,
límite que 3D.2 registró expresamente. Abrir antes 3D.5 obligaría a crear una identidad
de backup, una credencial y un destino de almacenamiento para proteger una base sin
datos, y produciría además un backup **sin restauración demostrada**, porque el canon
sitúa «restore» dentro de 3D.6. 3D.5 sigue **cerrada/no abierta**.

**Objetivo.** Demostrar contra la instancia PostgreSQL real, con datos exclusivamente
sintéticos y ejecución controlada, las garantías que quedaron configuradas y no
verificadas funcionalmente.

**Alcance propuesto.** Se ancla en el esquema realmente aplicado, no en supuestos: la
política `missions_scope_isolation` filtra por `current_setting('app.tenant_id')`,
`app.user_id` y `app.client_id`, con `ENABLE` + `FORCE ROW LEVEL SECURITY`; el control
optimista se apoya en la columna `version`, actualizada bajo `AND version = $5`; y
`mission_confirmations` añade unicidad por scope y campos de arrendamiento
(`consume_lease_*`).

| # | Prueba | Pertenencia al canon |
|---|---|---|
| A | Aislamiento funcional de RLS entre scopes/tenants sintéticos | 3D.2 lo difirió expresamente a 3D.6 |
| B | Imposibilidad de **lectura** cruzada entre scopes | Deriva de A |
| C | Imposibilidad de **escritura** cruzada (`WITH CHECK`) | Deriva de A |
| D | Control optimista de concurrencia (CAS sobre `version`) | «CAS» en la frontera 3D.2/3D.6 |
| E | Concurrencia relevante de Mission Queue, incluido el arrendamiento de confirmación | «concurrencia» en la frontera |
| F | Rollback transaccional | «rollback» en la frontera |
| G | Timeout y fallo controlado | «rollback/timeout» en la frontera |
| H | Limpieza/truncado seguro de los datos sintéticos | Necesario para revertir la propia prueba |
| I | Portabilidad y consistencia de las migraciones aplicadas | «portabilidad» en la frontera |
| J | Validación integral mínima de la persistencia real | «validación integral» en la frontera |
| K | No escalada: la identidad de runtime no puede desactivar ni eludir RLS, ni asumir o escalar al rol propietario. La garantía es el **rechazo efectivo**, no un código de error concreto: el SQLSTATE observado se registra como dato, nunca se predetermina. Cualquier resultado que permita bypass de RLS o escalada es **FAIL CLOSED** | Deriva del mínimo privilegio de 3D.2 |

Ninguna de estas pruebas amplía el alcance canónico: todas figuran en «Frontera 3D.2 /
3D.6» o se derivan directamente del mínimo privilegio ya exigido. **K** se hace
explícita porque una prueba de aislamiento que no compruebe la escalada demostraría
menos de lo que aparenta.

**Restore — DIFERIDO.** El canon incluye «restore» en 3D.6, pero 3D.5 no está abierta y
**no existe ningún artefacto de backup verificable**. Por tanto el componente restore
queda **BLOQUEADO/DIFERIDO** hasta disponer de un dump producido bajo el contrato de
3D.5. Interpretación propuesta, coherente con el precedente de esta fase —3D.2 se cerró
dejando registrado su límite y transfiriendo la validación funcional de RLS a 3D.6—:
3D.6 admite un **cierre parcial formal**, cubriendo A–K y dejando restore explícitamente
transferido, siempre que ese límite se registre en el cierre con la misma claridad. No
se decide aquí: se propone.

**Diseño de la sonda (diseño, no implementación).** Requisitos: fuera del arranque
productivo; sin reutilizar `server.js` ni ningún consumidor existente; datos
exclusivamente sintéticos; comprobación previa de que la identidad PostgreSQL conectada
es exactamente la esperada; TLS estricto con `rejectUnauthorized=true` y
`enableChannelBinding=true`; descomposición de la URL sin pasarla como
`connectionString`; percent-decode del userinfo conforme a la lección de 3D.4; fallo
cerrado ante cualquier ambigüedad; límites y `statement_timeout` explícitos;
transacciones acotadas; limpieza demostrable; salida saneada por lista blanca de campos
de error; nunca imprimir URL, contraseña, host completo ni payload; credenciales
temporales solo durante una ejecución autorizada y retiradas del entorno al terminar; y
**selftest 100 % offline en verde antes de cualquier conexión**.

**Ubicación recomendada: artefacto temporal fuera del repositorio**, en
`C:\Users\janta\AppData\Local\OXKIO\tools\`, con SHA-256 registrado en el canon, igual
que el runner de 3D.2 y las sondas de 3D.3 y 3D.4. Motivos: es el patrón ya validado
tres veces; mantiene fuera del árbol sincronizado por OneDrive un artefacto que
manipula credenciales temporales; evita que la sonda se convierta en código productivo
sin auditoría; y conserva la trazabilidad por hash. La alternativa de versionarla en el
repositorio se descarta por coherencia con los pendientes 10, 11 y 14 de TASKS, que
prohíben versionar estos artefactos en Git.

**Consumidores expresamente NO autorizados.** Los tests de integración de
`backend/services/mission-queue/` y el runner de `backend/repositories/poc/` construyen
`new Pool({ connectionString })` **sin `ssl` y sin `enableChannelBinding`**, en contra de
la política demostrada en 3D.3 y de la prohibición de pasar la URL como
`connectionString`. **No están autorizados a ejecutarse contra Neon real durante 3D.6**,
ni con PG-RUN ni con ninguna otra credencial. No se modifican aquí: su endurecimiento
exigiría auditoría y puerta propias. Sí pueden leerse como **especificación** de las
garantías a verificar, que es distinto de ejecutarlos.

**Criterios de entrada para la futura ejecución.** Contrato de 3D.6 aprobado por la
dirección; sonda diseñada e implementada con su selftest offline en **PASS** y SHA-256
registrado; TLS estricto y channel binding obligatorios; verificación previa de la
identidad PostgreSQL; dataset sintético y scopes/tenants sintéticos definidos por
escrito; plan exacto de limpieza; consultas de verificación definidas de antemano;
rollback definido; límite máximo de filas escritas fijado; salida saneada; cero datos
reales; cero consumidor productivo; y **segunda puerta humana concedida de forma
explícita**.

**Criterios de cierre — PASS/FAIL medibles.** No se aceptan «parece funcionar», «sin
errores» ni «conexión correcta». Cada garantía exige evidencia verificable y afirmativa:
en **A/B/C**, un scope sintético B debe obtener **cero filas** de los datos de A y su
intento de escritura sobre el scope de A debe ser **rechazado**, con el código de error
observado; en **K**, los intentos de `SET LOCAL row_security = off` y de asumir el rol
propietario desde la identidad de runtime deben resultar **efectivamente rechazados o
incapaces de producir la escalada**, y el SQLSTATE realmente devuelto se **registra como
dato observado**, saneado, sin fijarlo de antemano —predeterminar un código concreto
convertiría en FAIL un comportamiento seguro que devolviese otro error válido—; en
**D**, una actualización con `version` obsoleta debe no afectar filas mientras la
correcta sí lo hace; en **E**, dos operaciones concurrentes sobre la misma confirmación
deben producir exactamente un ganador; en **F/G**, un `ROLLBACK` y un timeout deben
dejar la tabla en su estado previo, comprobado por conteo; en **H**, el recuento final
de filas sintéticas debe ser **cero**; en **I/J**, la comprobación de catálogo debe
seguir coincidiendo con la verificación 33/33 de 3D.2.

**Fallo cerrado.** Si cualquier prueba falla, la ejecución se detiene, **no** se
improvisa ninguna corrección sobre la instancia real y el resultado vuelve a José
Antonio y a la dirección externa antes de cualquier otra acción.

**Anomalías registradas, no resueltas aquí.** (i) Existe solapamiento aparente entre
3D.5 —«Backup: `pg_dump`, destino, retención y rol de backup»— y la microfase 5C.7B.6
—«Backup, export, restore y DR; RPO/RTO; simulacro; restauración demostrada»—;
delimitación mínima propuesta: 3D.5 cubre la **producción y custodia** del dump y la
identidad de backup, y 5C.7B.6 cubre **DR, RPO/RTO y el simulacro de restauración**. No
se edita el contrato de 3D.5 ni se abre ninguna de las dos. (ii) **5C.7B.3F aparece como
fase cerrada/no abierta pero carece de contenido canónico localizable**: no se define,
no se abre y su resolución se transfiere a un turno posterior de gobernanza.

## Motivo del veredicto anterior REQUIERE CORRECCIÓN

El veredicto anterior no rechazaba la arquitectura candidata. Señalaba que el repositorio todavía no era desplegable con seguridad: conservaba tokens reales y credenciales en filesystem local, stores JSON incompatibles con múltiples instancias, rutas absolutas, puerto fijo, rutas legacy mezcladas y ausencia de recuperación demostrada.

La formulación fue demasiado amplia porque mezcló dos planos:

- correcciones técnicas necesarias antes de desplegar;
- selección todavía pendiente del runtime y de la persistencia.

Estas carencias no bloquean una preparación cloud-ready neutral. Sí bloquean cualquier despliegue, migración, activación de servicios o decisión definitiva de proveedor.

## Tratamiento del informe Antigravity

El primer informe ya fue recibido. Veredicto externo: **APROBADA CON CORRECCIONES**.

Riesgos válidos aceptados:

- stores JSON incompatibles con varias instancias;
- secretos locales;
- filesystem efímero;
- necesidad de idempotencia persistente;
- desacoplar trabajos duraderos;
- evitar sobreingeniería prematura.

No se aceptan como obligatorias sus recomendaciones de PostgreSQL inmediato, Redis/BullMQ inmediato ni Railway como destino decidido. Runtime y BBDD continúan pendientes de decisión basada en adaptadores, pruebas y métricas.

Errores técnicos o metodológicos detectados:

- convirtió necesidades futuras no medidas en componentes obligatorios;
- escogió runtime y BBDD antes de disponer de adaptadores, pruebas comparables y métricas;
- introdujo Redis/BullMQ sin carga, cola ni patrón de reintento que demostrara su necesidad;
- trató la corrección cloud-ready como si exigiera seleccionar proveedor;
- no preservó suficientemente la comparación neutral entre Firestore y PostgreSQL.

## Inventario real cloud-ready

| Componente | Evidencia | Clasificación | Destino |
|---|---|---|---|
| Backend Node HTTP | `backend/api/server.js`, puerto 3000, monolito | requires_adaptation | Contenedor; `PORT`, health y shutdown |
| Rutas ejecutivas | Chat, Dashboard, Identity, Approval, Execution y readonly | cloud_ready | Mantener contratos |
| Rutas `/api/chat`, `/api/execute`, reglas, memoria y logs | Mezcladas en el servidor oficial | deprecated | Cerrar antes de exposición remota |
| Frontend `app/` | Estáticos servidos mediante filesystem síncrono | requires_adaptation | Firebase Hosting o artefactos de contenedor |
| Firebase Authentication | Firebase Admin y allowlist UID | cloud_ready | Mantener; añadir tenant/roles |
| Firestore | POC aislada disponible; colecciones reales externas no inventariadas | unknown | No será BBDD operativa principal; conservar sin escritura |
| Gmail/Calendar OAuth | Integraciones reales y privadas | requires_adaptation | Gestor de secretos futuro por usuario |
| Gmail Draft | SAFE_DRAFT_ONLY y aprobación | cloud_ready | Conservar invariantes |
| Approval Queue | JSON local | local_only | Repositorio transaccional PostgreSQL ratificado |
| Execution Service | Depende de Approval Queue inyectada | cloud_ready | Inyectar repositorio remoto |
| Execution Log | `executionLog.json` | runtime_data | Logging + auditoría durable sanitizada |
| Memoria | `memory.json` | local_only | PostgreSQL por tenant/usuario ratificado |
| Knowledge | Objetos y conectores de carpetas locales | requires_adaptation | Metadatos PostgreSQL; blobs en autoridad externa |
| Supervisor | Registro canónico existente | canonical_documentation | Mantener uno, readonly |
| Ecosystem Observer | Composición readonly bajo demanda | cloud_ready | Mantener sin scheduler propio |
| Dashboard Intelligence | Vistas sanitizadas | cloud_ready | Mantener |
| Business Hunter | Servicio readonly con activos locales | requires_adaptation | Adaptadores remotos por tenant |
| Inicio | PowerShell/Batch y credencial Windows | local_only | Contrato de contenedor/pipeline |
| Configuración | JS fijo y variables locales | requires_adaptation | Config por entorno y Secret Manager |
| Token OAuth | `backend/auth/googleTokens.json`, ignorado por Git | secret | Revocar/rotar y sacar del filesystem |
| Credencial Firebase | Ruta `GOOGLE_APPLICATION_CREDENTIALS` | secret | Identidad de servicio IAM |
| Simulador/stores locales | JSON de laboratorio | runtime_data | No desplegar |
| Gobernanza | XANTALAL/orchestration | canonical_documentation | GitHub |
| `KNOWLEDGE-CURATOR` | Laboratorio separado | unknown | Fuera del runtime |

## Dependencias locales bloqueantes

1. Memoria, aprobaciones, logs, agenda, proyectos, seguridad y conocimiento escriben JSON local.
2. Existe un token OAuth real en filesystem local: debe revocarse/rotarse antes de desplegar.
3. Project Registry y Executive Agenda pueden escribir durante la inicialización del runtime.
4. Conectores de Knowledge/Business Hunter siguen dependiendo de filesystem local explícito.
5. No hay cola durable, reintentos ni dead-letter para segundo plano.
6. Rutas legacy y oficiales comparten servidor.
7. Las colecciones Firestore reales no están inventariadas ni clasificadas.
8. El POC validó aislamiento y restore, pero aún no existe integración multiusuario productiva.

## Runtime candidato y persistencia ratificada

Cloud Run + Firebase Authentication continúa como runtime candidato. Tras ejecutar el
mismo harness contra Firestore Emulator y PostgreSQL portable, la decisión humana
ratifica PostgreSQL gestionado como persistencia operativa principal. G0002.5B.2F
selecciona arquitectónicamente Neon Launch en Frankfurt; no selecciona plan de pago ni
runtime y no autoriza gasto. Cloud SQL Enterprise queda como contingencia. El alojamiento
compartido actual de LucusHost no es apto porque no admite PostgreSQL remoto.

### Runtime

- Si se aprueba tras auditoría, Cloud Run ejecutaría el contenedor Node.
- `minInstances=1` solo durante el piloto que requiera evitar cold start; revisar con métricas.
- `maxInstances` bajo y concurrencia conservadora hasta que Approval Queue sea transaccional.
- El proceso escucha `process.env.PORT` y no escribe estado durable en disco.
- Firebase Hosting podría servir el frontend y dirigir `/api/**` al runtime escogido.

### Identidad y datos

- Firebase Authentication permanece como identidad.
- El backend deriva `tenantId` y `userId` del token verificado; nunca confía en el body.
- PostgreSQL gestionado es la fuente futura ratificada para tenant/membresías, perfiles,
  memoria, tareas internas, operaciones, aprobaciones, idempotencia, auditoría, stores
  ejecutivos y metadatos de documentos/conocimiento.
- Ninguna BBDD contiene binarios, código, secretos, tokens en claro, logs de alta
  cardinalidad, cachés ni payloads sin sanitizar.
- Cloud Storage/Drive conserva binarios y documentos; PostgreSQL solo sus metadatos.
- GitHub conserva solo código, infraestructura declarativa y gobernanza.

### Secretos y OAuth

- El gestor de secretos futuro guarda secretos de aplicación, credenciales y OAuth.
- Cloud Run usa identidad de servicio IAM, no un fichero service-account.
- Para Cliente Cero, los tokens OAuth estarán cifrados y con acceso mínimo dentro del
  gestor de secretos futuro. Antes de multiusuario se aprobará cifrado/KMS y rotación;
  nunca se guardarán en claro ni en PostgreSQL.

### Segundo plano, recuperación y despliegue

- Cloud Tasks: trabajos por evento con reintentos e idempotencia.
- Cloud Scheduler + Cloud Run Jobs: tareas programadas finitas.
- Pub/Sub solo con necesidad demostrada.
- No ejecutar schedulers críticos dentro del proceso HTTP.
- GitHub Actions: pruebas, checks, build, scan, staging, smoke y promoción humana.
- Rollback mediante revisión anterior de Cloud Run.
- Logs JSON sin contenido privado y correlación por `interactionId`, `approvalId` y `executionId`.
- Cloud Monitoring: disponibilidad, latencia, errores, duplicados bloqueados, cola y consumo.

### Invariantes

- `executionEnabled=false` por defecto y fail-closed.
- `draftExecutionEnabled` separado y limitado a Gmail Draft.
- `SAFE_DRAFT_ONLY` y `allowRealSend=false` son invariantes de arranque y ejecución.
- Configuración ausente o contradictoria impide iniciar el ejecutor.

## Resultado implementado de 5C.7B.1

- `PORT` validado y configurable; host `0.0.0.0`.
- `/health` y `/ready` independientes de autenticación.
- shutdown idempotente para `SIGTERM` y `SIGINT`, readiness cerrado antes del servidor y cleanup posterior.
- inicio estándar mediante `node backend/api/server.js`; PowerShell queda como ayuda Windows opcional.
- defaults absolutos Windows y dependencia obligatoria de OneDrive eliminados del runtime auditado.
- filesystem declarado `ephemeral`.
- Dockerfile reproducible, multi-stage, usuario no root y sin secretos/stores en la imagen.
- contrato centralizado y validado de variables de entorno, sin imprimir valores sensibles.
- contratos neutrales para Approval, Memory, Operation, Audit y OAuth Token.
- adaptadores JSON conservados y marcados `local_only`.
- ninguna migración, proveedor o servicio cloud activado.

## Evidencia del piloto local cloud-ready

- Node estándar arrancó en `0.0.0.0` con `PORT=3000` y `PORT=3107`.
- `/health` y `/ready` devolvieron `200` sin información sensible.
- Executive Dashboard devolvió `200`; la API protegida mantuvo Firebase Authentication y respondió `401` sin token.
- SAFE_DRAFT_ONLY permaneció activo, `executionEnabled=false` y `allowRealSend=false`.
- SIGTERM y SIGINT cerraron los procesos; ambos puertos quedaron liberados.
- Los hashes de Approval Queue, Memory y Execution Log permanecieron sin cambios.
- Docker no está instalado en el entorno local: construcción y ejecución de imagen bloqueadas por entorno, sin simulación.

## Matriz de capacidades de datos

| Repositorio | Operaciones | Atomicidad/concurrencia | Consultas y volumen | Retención | Relacional/vectorial | Tenant e idempotencia | Exportación/restauración |
|---|---|---|---|---|---|---|---|
| ApprovalRepository | snapshot; futura transición condicional | transición atómica; alta contención por aprobación | id, estado, fecha; bajo-medio | por definir | relación con operación; no vector | tenant; clave por efecto | snapshot consistente |
| MemoryRepository | snapshot; futura ventana/búsqueda | escritura sin pérdida | usuario, tiempo, tipo; continuo | configurable | vector futuro no obligatorio | tenant; deduplicación | export por tenant |
| OperationRepository | registrar/listar/terminal | append y transición atómica | ids, estado, fechas; medio | operativa/auditoría | relación útil; no vector | tenant e `idempotencyKey` | restore verificable |
| AuditRepository | append/listar | append-only ordenado | actor, acción, fecha; medio-alto | legal pendiente | relación útil; no vector | tenant; evento único | export inmutable |
| OAuthTokenRepository | cargar/guardar/eliminar sujeto | compare-and-set y rotación | sujeto/proveedor; bajo | caducidad/revocación | ninguna | sujeto/tenant | reautorización; nunca claro |
La matriz de contratos permanece neutral, pero la ADR de 5C.7B.2 selecciona PostgreSQL
gestionado como persistencia operativa principal. Los adaptadores productivos todavía
no están implementados.

## Contrato centralizado de variables de entorno

| Variable | Clasificación | Uso |
|---|---|---|
| `PORT` | optional | puerto HTTP; 3000 local por defecto |
| `NODE_ENV` | optional | modo Node validado |
| `OXKIO_FILESYSTEM_MODE` | invariant | únicamente `ephemeral` fuera del adaptador local |
| `XANTALAL_ROOT` | optional, local_only | conector local de gobernanza |
| `KNOWLEDGE_DISCOVERY_ROOT` | optional, local_only | descubrimiento local explícito |
| `GOOGLE_CLIENT_ID` | required | capacidad Google OAuth |
| `GOOGLE_CLIENT_SECRET` | required, secret | capacidad Google OAuth |
| `GOOGLE_REDIRECT_URI` | required | capacidad Google OAuth |
| `FIREBASE_PROJECT_ID` | required | capacidad Firebase |
| `FIREBASE_CLIENT_EMAIL` | optional, secret | credencial individual Firebase |
| `FIREBASE_PRIVATE_KEY` | optional, secret | credencial individual Firebase |
| `GOOGLE_APPLICATION_CREDENTIALS` | optional, local_only, secret | compatibilidad Cliente Cero |
| `GOOGLE_CLOUD_PROJECT` | optional | credenciales por defecto |
| `OXKIO_ADMIN_FIREBASE_UIDS` | required, secret | autorización ejecutiva |
| `OXKIO_ADMIN_FIREBASE_EMAILS` | optional, secret | allowlist auxiliar |
| `OPENAI_API_KEY` | optional, local_only, secret | simulador, fuera del runtime oficial |

La obligatoriedad se valida al activar cada capacidad, para conservar el arranque local sin convertir integraciones opcionales en requisitos globales. Los diagnósticos solo contienen nombres de variables.

## Inventario de stores y filesystem

| Store o acceso | Clasificación | Tratamiento |
|---|---|---|
| `backend/core/approvalQueue.json` | productivo local, runtime, migración futura | `ApprovalRepository`; no migrado |
| `backend/memory/memory.json` | productivo local, runtime, migración futura | `MemoryRepository`; no migrado |
| `backend/core/executionLog.json` | runtime, auditoría, migración futura | `OperationRepository`/`AuditRepository`; no tocar datos |
| stores de `strategicMemory`, `executiveAgenda`, `projectRegistry` y `securityInventory` | productivo local, runtime, migración futura | inventariados; extracción posterior |
| `backend/knowledge/knowledgeStore.json` y `backend/data/knowledge-store/objects/*.json` | productivo local, runtime, migración futura | metadatos/objetos; extracción posterior |
| estado de `knowledge-change-detector` | runtime, migración futura | estado durable pendiente |
| `backend/auth/googleTokens.json` | secreto, productivo local, migración futura | no leído ni modificado; sacar del filesystem antes de desplegar |
| `approvalQueue.example.json`, `memory.example.json` | ejemplo | mantener fuera de persistencia productiva |
| JSON creados en temporales por `*.test.js` | test fixture | mantener aislados |
| stores de `simulador-ia` | runtime local/laboratorio | excluir de imagen y runtime oficial |
| XANTALAL y `orchestration/*.md` | gobernanza | fuente canónica Git; no es store operativo |
| lectura de estáticos `app/` | artefacto de aplicación | permitida readonly dentro de imagen |
| conectores de carpetas locales/OneDrive | local_only | requieren ruta explícita; no dependencia de arranque |

## Contrato neutral de idempotencia

- `idempotencyKey` estable por intención autorizada y efecto externo.
- Estados: `reserved`, `executing`, `succeeded`, `failed_retryable`, `failed_terminal`, `external_effect_unknown`.
- La reserva y cada transición futura deberán ser atómicas.
- `succeeded`, `failed_terminal` y `external_effect_unknown` son terminales.
- `failed_retryable` permite volver a `executing` solo bajo política explícita.
- Un efecto externo incierto nunca se reintenta automáticamente.
- El resultado externo persistido es mínimo y sanitizado.
- La clave incorpora sujeto/tenant y no contiene secretos.

## Alternativa secundaria candidata

Railway permite alojar con poca fricción el contenedor Node, cron y workers. Mantendría
Firebase Authentication y conectaría con el PostgreSQL gestionado que se seleccione. Es
una alternativa de runtime, no una selección de BBDD; su plantilla PostgreSQL está
documentada como `unmanaged` y no satisface por sí sola el requisito gestionado.

## Comparativa de runtimes

| Opción | Node/procesos | Jobs/WebSockets | Operación/coste | Decisión |
|---|---|---|---|---|
| Cloud Run | Alta tras `PORT`; disco efímero | Jobs, Scheduler/Tasks; WebSockets | Gestionado, revisiones, pago por uso | Candidata principal pendiente |
| Firebase App Hosting | Media; más orientado a frameworks web; usa Cloud Run | No sustituye workers | GitHub/CDN; Blaze obligatorio | Frontend futuro |
| Firebase Functions | Handlers stateless | Eventos/programación | Gestionado, más acoplamiento | Solo funciones puntuales |
| Railway | Alta; volúmenes/servicios activos | Cron, workers, colas, WebSockets | Desde $5/mes + uso | Alternativa secundaria |
| Render | Alta; disco efímero por defecto | Workers, cron, WebSockets | Simple; precio según plan | Alternativa terciaria |
| VPS gestionado/propio | Alta y persistente | Libertad total | Parches, HA, backups y TLS propios | No ahora |
| LucusHost | Condicionado al propio hosting | Sin PostgreSQL remoto | Sin PITR ni restore PostgreSQL específico | No apto para runtime externo |
| Netlify | Frontend/functions | No asumir runtime/WebSocket persistente | Adecuado para frontend | No runtime principal |

Fuentes oficiales: [Cloud Run](https://docs.cloud.google.com/run/docs/overview/what-is-cloud-run), [precios Cloud Run](https://cloud.google.com/run/pricing), [App Hosting](https://firebase.google.com/docs/app-hosting), [costes App Hosting](https://firebase.google.com/docs/app-hosting/costs), [Railway](https://docs.railway.com/pricing/plans), [workers Railway](https://docs.railway.com/guides/cron-workers-queues), [workers Render](https://render.com/docs/background-workers) y [discos Render](https://render.com/docs/disks).

## Comparativa de BBDD

| Opción | Evaluación | Decisión |
|---|---|---|
| Firestore | POC superada; documental y transaccional; exige más invariantes de aplicación para el núcleo | No será BBDD operativa principal |
| PostgreSQL gestionado | POC superada; ACID, constraints, RLS, append-only, reporting y portabilidad | Persistencia principal ratificada |
| Híbrida Firestore + PostgreSQL | Dos fuentes, sin transacción distribuida simple y recuperación doble | Descartada para el núcleo |
| Neon Launch | PostgreSQL serverless, pooling y crecimiento por uso; Frankfurt | Ganador arquitectónico de Fase 1.2 |
| Cloud SQL PostgreSQL | PostgreSQL gestionado con backups/PITR e integración Google | Contingencia |
| Supabase | PostgreSQL gestionado; RPO aprobado exige un coste desproporcionado para Cliente Cero | Tercer candidato |
| PostgreSQL LucusHost compartido | Sin acceso remoto, PITR ni restauración específica | No apto para runtime externo |
| MySQL/MariaDB LucusHost | Motor distinto del ratificado | No cambiar PostgreSQL para aprovechar el hosting |
| SQLite | Archivo único y contención multiinstancia | Solo laboratorio |

No crear ni migrar BBDD durante 5C.7B.2. La decisión de motor está registrada en
`XANTALAL/00_GOVERNANCE/ADR-5C.7B.2-POSTGRESQL-PERSISTENCIA-PRINCIPAL.md`; la evidencia
detallada está en
`XANTALAL/00_GOVERNANCE/5C.7B.2-PERSISTENCIA-DEFINITIVA.md`.

## Mapa de datos

| Dato | Fuente canónica/propietario | Lectores/escritores | Sensibilidad | Retención, copia y restauración | Actual → objetivo |
|---|---|---|---|---|---|
| Identidad | Firebase Auth/usuario | backend/Firebase | alta | política Firebase | Firebase → Firebase |
| Perfil/tenant | PostgreSQL/tenant | backend/admin | alta | backup y restore | inexistente → PostgreSQL |
| Memoria | PostgreSQL/usuario | Brain/Memory | alta | configurable; export | JSON → PostgreSQL |
| Tareas internas | PostgreSQL/tenant | Dashboard/servicio futuro | media/alta | por definir | contrato pendiente → PostgreSQL |
| Calendario de usuario | Google Calendar/usuario | Calendar service | alta | proveedor + export | Calendar → Calendar |
| Agenda ejecutiva interna | PostgreSQL/tenant | Supervisor/servicios ejecutivos | alta | backup/export | JSON → PostgreSQL |
| Documentos | Drive/Storage/usuario | Knowledge/importadores | alta | versionado + copia | local/Drive → Drive/Storage |
| Metadatos de documentos/conocimiento | PostgreSQL/tenant | Knowledge/pipeline | media | backup/export | local → PostgreSQL, migración posterior |
| Project Registry | PostgreSQL/tenant | Supervisor/Project Manager | alta | backup/export | JSON → PostgreSQL |
| Security Inventory | PostgreSQL/tenant | Supervisor/Security Auditor | crítica | backup/export restringido | JSON → PostgreSQL |
| Operaciones | PostgreSQL/tenant | Supervisor/Coordinator | alta | retención + export | memoria/JSON → PostgreSQL |
| Aprobaciones | PostgreSQL/tenant | Dashboard/Queue | crítica | transacción, backup, restore | JSON → PostgreSQL |
| Auditorías | PostgreSQL append-only + export | auditor/servicios | crítica | retención por definir | inexistente/JSON → PostgreSQL/export |
| Logs | Cloud Logging/XANTALAL | operación/runtime | media | 30 días iniciales | consola/JSON → Logging |
| Conocimiento | PostgreSQL metadatos + fuente documental externa | Knowledge/Pipeline | variable | versionado/export | filesystem → metadatos + autoridad externa |
| Tokens OAuth | gestor de secretos futuro/usuario | OAuth service | crítica | hasta revocación | JSON → gestor de secretos; fuera de BBDD |
| Config no secreta | GitHub/XANTALAL | runtime/CI | baja/media | Git | JS → entorno |
| Secretos | Secret Manager/XANTALAL | identidad de servicio | crítica | versiones/rotación | fichero/entorno → SM |
| Gobernanza | GitHub/XANTALAL | proyecto/humano | interna | Git + copia Drive | GitHub → GitHub |

## Costes conocidos y desconocidos

- Firestore publica 1 GiB, 50.000 lecturas/día, 20.000 escrituras/día,
  20.000 borrados/día y 10 GiB/mes de salida en la cuota gratuita de una base por
  proyecto; backup, PITR, restore, clones y TTL requieren facturación.
- PostgreSQL gestionado se factura según proveedor, región, CPU/RAM, storage/IOPS,
  conexiones, HA, backup/PITR y red; sin esos parámetros el importe es `unknown`.
- Runtime, Railway, Render, VPS y LucusHost quedaron `unknown` en 5C.7B.2. La decisión
  posterior de G0002.5B.2F selecciona Neon y descarta el hosting compartido actual de
  LucusHost para PostgreSQL con runtime externo.
- Costes para 10, 100 y 1.000 usuarios: `unknown`. Hay métricas locales de POC, pero no
  un patrón cloud ni una configuración comparable que permita estimarlos.

## Riesgos

1. Token OAuth real local: crítico; revocar/rotar.
2. JSON local incompatible con múltiples instancias y transacciones.
3. Rutas legacy amplían superficie de ataque.
4. Backend, estáticos y laboratorio mezclados.
5. El aislamiento tenant está probado en POC, pero no integrado en el runtime.
6. Restore local demostrado; RPO/RTO y restore gestionado pendientes.
7. Coste desconocido sin telemetría.
8. Dependencia Google; mitigar con contenedores, repositorios y exportaciones, no duplicando BBDD.
9. Background sin cola durable.
10. LucusHost compartido no admite PostgreSQL remoto y no cumple la recuperación aprobada.

## Plan de implementación

| Subfase | Objetivo/archivos | Riesgos y pruebas | Cierre/rollback/autoridad |
|---|---|---|---|
| 5C.7B.1 | Preparación cloud-ready neutral | contratos, suite y contenedor local | artefacto reproducible, sin migración ni proveedor activado |
| 5C.7B.2 | Inventario, POC comparable y ADR | ambos motores/restore aprobados; proveedor y coste pendientes | cerrada y publicada; PostgreSQL gestionado ratificado |
| G0002.5B.2E | Commit atómico Confirmation → Mission | PostgreSQL Integration, contratos y servicios: 179/179 | cerrada y publicada en `e4c79ff`; no abre la siguiente microfase |
| G0002.5B.2F | Arquitectura productiva PostgreSQL para Cliente Cero | sobre de carga, selección, arquitectura operativa y consolidación aprobados; sin gasto | cerrada arquitectónicamente; Fases 1.1–1.4 cerradas |
| 5C.7B.3 | Secret Manager, IAM, tokens cifrados y rotación | fuga/pérdida de acceso | abierta; 3A–C cerradas; 3D abierta como contenedor (3D.1–3D.4 cerradas, con `verify` 33/33, T1–T5 superadas y PG-RUN materializado; 3D.5 cerrada/no abierta; 3D.6 abierta solo en planificación documental); 3E–F cerradas |
| 5C.7B.4 | Staging Cloud Run y pipeline | coste/config; smoke/rollback | staging estable; autorizar Blaze/despliegue |
| 5C.7B.5 | Logs, métricas, alertas y presupuesto | privacidad; redacción/alertas | observabilidad verificada |
| 5C.7B.6 | Backup, export, restore y DR | RPO/RTO; simulacro | restauración demostrada |
| 5C.7B.7 | Piloto Cliente Cero remoto | OAuth, latencia, duplicados | 24/7 observado; draft único; cero envíos |
| 5C.7B.8 | Auditoría Antigravity | hallazgos | sin `CORREGIR`; decisión humana |
| 5C.7B.9 | Probadores | aislamiento/soporte | tenant isolation, consentimiento y rollback |

## Preguntas para LucusHost

1. Plan contratado, renovación y coste total con IVA/excesos.
2. Node 24/7, versión, `PORT`, Docker, SSH, systemd/PM2 y auto-restart.
3. CPU, RAM, disco, IOPS, tráfico, concurrencia y procesos.
4. Persistencia entre despliegues y almacenamiento de objetos.
5. Secretos, rotación, IAM y logs.
6. Cron, workers, colas, WebSockets y límites.
7. BBDD, versiones, conexiones, TLS, backups, PITR y exportación.
8. Regiones, SLA, monitorización, alertas, rollback y soporte.
9. Salida de código, datos, dominios y certificados.
10. DPA/RGPD, ubicación y subprocesadores.

## Puntos de auditoría Antigravity

### Auditoría 1 — selección arquitectónica

Completada antes de seleccionar runtime y persistencia. Veredicto: **APROBADA CON CORRECCIONES**. Sus riesgos válidos quedan incorporados y sus elecciones tecnológicas no son obligatorias. No bloquea 5C.7B.1.

### Auditoría 2 — aceptación operativa

Ejecutar al cierre de 5C.7B.7 y antes de probadores. Prerrequisitos: piloto remoto, tokens rotados, persistencia transaccional, tenant probado, observabilidad, restore, SAFE_DRAFT_ONLY y costes medidos.

## Alcance cerrado de 5C.7B.1

Incluye exclusivamente:

1. `PORT` configurable.
2. Health y readiness.
3. Shutdown controlado.
4. Eliminación de rutas absolutas.
5. Contrato de variables de entorno.
6. Filesystem tratado como efímero.
7. Contenedor reproducible y neutral.
8. Inventario de stores que deberán extraerse.

Excluye:

- migración de datos o stores;
- selección definitiva de runtime o BBDD;
- activación de Cloud Run, Blaze, Firestore nuevo, Secret Manager o facturación;
- contratación de servicios;
- despliegue;
- modificación de SAFE_DRAFT_ONLY o flags de ejecución.

## Próxima acción exacta

5C.7B.3C, 3D.1 y 3D.2 están cerradas. 3D.2 cierra con roles creados por SQL
controlado, migraciones 001/002 aplicadas y `verify` 33/33 en solo lectura, sin
escribir ninguna fila y sin demostrar todavía aislamiento funcional entre scopes.

**5C.7B.3D.3 — TLS/SSL estricto** queda **cerrada**, con T1–T5 ejecutadas y
superadas: CA y hostname verificados con fallo cerrado en ambos casos,
`rejectUnauthorized=true`, certificado vigente, `SCRAM-SHA-256-PLUS` demostrado
positivamente y anti-downgrade validado offline. Todo ello **mediante sonda**; el
runtime productivo sigue sin política TLS cableada, pendiente transversal que deberá
resolverse antes de cualquier conexión o despliegue productivo.

**5C.7B.3D.4 — Secretos PostgreSQL reales en Secret Manager**, ligados a las tres
service accounts existentes de 3C.2, queda **CERRADA** el 15/08/2026. El inventario de
las tres service accounts se resolvió en solo lectura el 14/08/2026; el contrato de
PG-RUN quedó fijado; y ese día se ejecutaron manualmente, bajo puerta humana concedida
y ya **consumida**, las Puertas A y B: `OXKIO_MISSION_PG_RUNTIME_URL` existe en
`oxkio-runtime-prod` con Version 1 habilitada, replicación automática y un binding
explícito de acceso a secretos, a nivel del propio secreto, para la service account de
runtime.

Tras evaluar las candidatas, **5C.7B.3D.6 queda abierta solo en planificación
documental**; su contrato está fijado más arriba y su ejecución exige una segunda
puerta humana. Ninguna otra acción queda habilitada: nuevas versiones, cambios de IAM,
PG-MIG, PG-BKP, TLS productivo, `environment-contract.js`, `server.js`, despliegue, rol
de backup, pruebas con escritura, datos reales, contratación o gasto exigen cada uno su
propia puerta humana. 3D.5 y 5C.7B.3E–F continúan cerradas/no abiertas.
