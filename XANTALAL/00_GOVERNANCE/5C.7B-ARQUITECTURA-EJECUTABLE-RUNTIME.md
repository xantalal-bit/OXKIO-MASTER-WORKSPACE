# 5C.7B — ARQUITECTURA EJECUTABLE DEL RUNTIME

## Estado

- Fecha: 27/07/2026.
- Alcance: implementación cloud-ready neutral; sin despliegue ni migración.
- Base: 5C.6D.1 publicada en `98b2998`.
- Candidata principal pendiente de auditoría externa: Cloud Run + Firebase Authentication + Firestore.
- Alternativa secundaria: Railway + Firebase Authentication + Firestore.
- Veredicto actual: LISTO PARA PILOTO LOCAL CLOUD-READY.

## Motivo del veredicto anterior REQUIERE CORRECCIÓN

El veredicto anterior no rechazaba la arquitectura candidata. Señalaba que el repositorio todavía no era desplegable con seguridad: conservaba tokens reales y credenciales en filesystem local, stores JSON incompatibles con múltiples instancias, rutas absolutas, puerto fijo, rutas legacy mezcladas y ausencia de recuperación demostrada.

La formulación fue demasiado amplia porque mezcló dos planos:

- correcciones técnicas necesarias antes de desplegar;
- selección todavía pendiente del runtime y de la persistencia.

Estas carencias no bloquean una preparación cloud-ready neutral. Sí bloquean cualquier despliegue, migración, activación de servicios o decisión definitiva de proveedor.

## Tratamiento del informe Antigravity

El primer informe ya fue recibido. Veredicto externo: **APROBADA CON CORRECCIONES**.

Riesgos válidos aceptados:

- stores JSON incompatibles con varias instancias;
- secretos locales;
- filesystem efímero;
- necesidad de idempotencia persistente;
- desacoplar trabajos duraderos;
- evitar sobreingeniería prematura.

No se aceptan como obligatorias sus recomendaciones de PostgreSQL inmediato, Redis/BullMQ inmediato ni Railway como destino decidido. Runtime y BBDD continúan pendientes de decisión basada en adaptadores, pruebas y métricas.

Errores técnicos o metodológicos detectados:

- convirtió necesidades futuras no medidas en componentes obligatorios;
- escogió runtime y BBDD antes de disponer de adaptadores, pruebas comparables y métricas;
- introdujo Redis/BullMQ sin carga, cola ni patrón de reintento que demostrara su necesidad;
- trató la corrección cloud-ready como si exigiera seleccionar proveedor;
- no preservó suficientemente la comparación neutral entre Firestore y PostgreSQL.

## Inventario real cloud-ready

| Componente | Evidencia | Clasificación | Destino |
|---|---|---|---|
| Backend Node HTTP | `backend/api/server.js`, puerto 3000, monolito | requires_adaptation | Contenedor; `PORT`, health y shutdown |
| Rutas ejecutivas | Chat, Dashboard, Identity, Approval, Execution y readonly | cloud_ready | Mantener contratos |
| Rutas `/api/chat`, `/api/execute`, reglas, memoria y logs | Mezcladas en el servidor oficial | deprecated | Cerrar antes de exposición remota |
| Frontend `app/` | Estáticos servidos mediante filesystem síncrono | requires_adaptation | Firebase Hosting o artefactos de contenedor |
| Firebase Authentication | Firebase Admin y allowlist UID | cloud_ready | Mantener; añadir tenant/roles |
| Firestore | Colecciones aceptadas externas; sin adaptador en el repositorio | requires_adaptation | Fuente operativa tras contrato y pruebas |
| Gmail/Calendar OAuth | Integraciones reales y privadas | requires_adaptation | Tokens cifrados por usuario |
| Gmail Draft | SAFE_DRAFT_ONLY y aprobación | cloud_ready | Conservar invariantes |
| Approval Queue | JSON local | local_only | Repositorio Firestore transaccional |
| Execution Service | Depende de Approval Queue inyectada | cloud_ready | Inyectar repositorio remoto |
| Execution Log | `executionLog.json` | runtime_data | Logging + auditoría durable sanitizada |
| Memoria | `memory.json` | local_only | Firestore por tenant |
| Knowledge | Objetos y conectores de carpetas locales | requires_adaptation | Metadatos Firestore; blobs Storage/Drive |
| Supervisor | Registro canónico existente | canonical_documentation | Mantener uno, readonly |
| Ecosystem Observer | Composición readonly bajo demanda | cloud_ready | Mantener sin scheduler propio |
| Dashboard Intelligence | Vistas sanitizadas | cloud_ready | Mantener |
| Business Hunter | Servicio readonly con activos locales | requires_adaptation | Adaptadores remotos por tenant |
| Inicio | PowerShell/Batch y credencial Windows | local_only | Contrato de contenedor/pipeline |
| Configuración | JS fijo y variables locales | requires_adaptation | Config por entorno y Secret Manager |
| Token OAuth | `backend/auth/googleTokens.json`, ignorado por Git | secret | Revocar/rotar y sacar del filesystem |
| Credencial Firebase | Ruta `GOOGLE_APPLICATION_CREDENTIALS` | secret | Identidad de servicio IAM |
| Simulador/stores locales | JSON de laboratorio | runtime_data | No desplegar |
| Gobernanza | XANTALAL/orchestration | canonical_documentation | GitHub |
| `KNOWLEDGE-CURATOR` | Laboratorio separado | unknown | Fuera del runtime |

## Dependencias locales bloqueantes

1. Memoria, aprobaciones, logs, agenda, proyectos, seguridad y conocimiento escriben JSON local.
2. Existe un token OAuth real en filesystem local: debe revocarse/rotarse antes de desplegar.
3. Firebase Admin depende de una ruta Windows a un JSON.
4. Puerto fijo, arranque PowerShell y rutas absolutas OneDrive/XANTALAL.
5. No hay cola durable, reintentos ni dead-letter para segundo plano.
6. Rutas legacy y oficiales comparten servidor.
7. Firestore no tiene repositorios, índices, reglas ni pruebas de aislamiento en este código.
8. No existen health/readiness, política cloud de concurrencia ni restauración probada.

## Arquitectura candidata principal pendiente de auditoría externa

Cloud Run + Firebase Authentication + Firestore constituye una candidata técnica, no una decisión definitiva. El primer informe Antigravity ya fue recibido y no convierte esa candidata ni sus alternativas en decisiones. LucusHost permanece `unknown` hasta obtener datos reales.

### Runtime

- Si se aprueba tras auditoría, Cloud Run ejecutaría el contenedor Node.
- `minInstances=1` solo durante el piloto que requiera evitar cold start; revisar con métricas.
- `maxInstances` bajo y concurrencia conservadora hasta que Approval Queue sea transaccional.
- El proceso escucha `process.env.PORT` y no escribe estado durable en disco.
- Firebase Hosting podría servir el frontend y dirigir `/api/**` al runtime escogido.

### Identidad y datos

- Firebase Authentication permanece como identidad.
- El backend deriva `tenantId` y `userId` del token verificado; nunca confía en el body.
- Firestore es candidata para tareas, memoria estructurada, operaciones, aprobaciones, auditorías sanitizadas y metadatos; no queda seleccionada definitivamente.
- Firestore no contiene binarios, código, secretos, tokens en claro, logs de alta cardinalidad, cachés ni payloads sin sanitizar.
- Cloud Storage conserva blobs de aplicación; Drive conserva documentos de usuario, exportaciones y copias entregables.
- GitHub conserva solo código, infraestructura declarativa y gobernanza.

### Secretos y OAuth

- Secret Manager guarda secretos de aplicación y credenciales rotables.
- Cloud Run usa identidad de servicio IAM, no un fichero service-account.
- Para Cliente Cero, tokens OAuth cifrados y con acceso mínimo. Antes de multiusuario se aprobará el diseño de cifrado/KMS; nunca se guardarán en claro.

### Segundo plano, recuperación y despliegue

- Cloud Tasks: trabajos por evento con reintentos e idempotencia.
- Cloud Scheduler + Cloud Run Jobs: tareas programadas finitas.
- Pub/Sub solo con necesidad demostrada.
- No ejecutar schedulers críticos dentro del proceso HTTP.
- GitHub Actions: pruebas, checks, build, scan, staging, smoke y promoción humana.
- Rollback mediante revisión anterior de Cloud Run.
- Logs JSON sin contenido privado y correlación por `interactionId`, `approvalId` y `executionId`.
- Cloud Monitoring: disponibilidad, latencia, errores, duplicados bloqueados, cola y consumo.

### Invariantes

- `executionEnabled=false` por defecto y fail-closed.
- `draftExecutionEnabled` separado y limitado a Gmail Draft.
- `SAFE_DRAFT_ONLY` y `allowRealSend=false` son invariantes de arranque y ejecución.
- Configuración ausente o contradictoria impide iniciar el ejecutor.

## Resultado implementado de 5C.7B.1

- `PORT` validado y configurable; host `0.0.0.0`.
- `/health` y `/ready` independientes de autenticación.
- shutdown idempotente para `SIGTERM` y `SIGINT`, readiness cerrado antes del servidor y cleanup posterior.
- inicio estándar mediante `node backend/api/server.js`; PowerShell queda como ayuda Windows opcional.
- defaults absolutos Windows y dependencia obligatoria de OneDrive eliminados del runtime auditado.
- filesystem declarado `ephemeral`.
- Dockerfile reproducible, multi-stage, usuario no root y sin secretos/stores en la imagen.
- contrato centralizado y validado de variables de entorno, sin imprimir valores sensibles.
- contratos neutrales para Approval, Memory, Operation, Audit y OAuth Token.
- adaptadores JSON conservados y marcados `local_only`.
- ninguna migración, proveedor o servicio cloud activado.

## Evidencia del piloto local cloud-ready

- Node estándar arrancó en `0.0.0.0` con `PORT=3000` y `PORT=3107`.
- `/health` y `/ready` devolvieron `200` sin información sensible.
- Executive Dashboard devolvió `200`; la API protegida mantuvo Firebase Authentication y respondió `401` sin token.
- SAFE_DRAFT_ONLY permaneció activo, `executionEnabled=false` y `allowRealSend=false`.
- SIGTERM y SIGINT cerraron los procesos; ambos puertos quedaron liberados.
- Los hashes de Approval Queue, Memory y Execution Log permanecieron sin cambios.
- Docker no está instalado en el entorno local: construcción y ejecución de imagen bloqueadas por entorno, sin simulación.

## Matriz de capacidades de datos

| Repositorio | Operaciones | Atomicidad/concurrencia | Consultas y volumen | Retención | Relacional/vectorial | Tenant e idempotencia | Exportación/restauración |
|---|---|---|---|---|---|---|---|
| ApprovalRepository | snapshot; futura transición condicional | transición atómica; alta contención por aprobación | id, estado, fecha; bajo-medio | por definir | relación con operación; no vector | tenant; clave por efecto | snapshot consistente |
| MemoryRepository | snapshot; futura ventana/búsqueda | escritura sin pérdida | usuario, tiempo, tipo; continuo | configurable | vector futuro no obligatorio | tenant; deduplicación | export por tenant |
| OperationRepository | registrar/listar/terminal | append y transición atómica | ids, estado, fechas; medio | operativa/auditoría | relación útil; no vector | tenant e `idempotencyKey` | restore verificable |
| AuditRepository | append/listar | append-only ordenado | actor, acción, fecha; medio-alto | legal pendiente | relación útil; no vector | tenant; evento único | export inmutable |
| OAuthTokenRepository | cargar/guardar/eliminar sujeto | compare-and-set y rotación | sujeto/proveedor; bajo | caducidad/revocación | ninguna | sujeto/tenant | reautorización; nunca claro |
La matriz no selecciona Firestore, PostgreSQL ni otra BBDD. Las cifras se obtendrán con pruebas de adaptadores y métricas.

## Contrato centralizado de variables de entorno

| Variable | Clasificación | Uso |
|---|---|---|
| `PORT` | optional | puerto HTTP; 3000 local por defecto |
| `NODE_ENV` | optional | modo Node validado |
| `OXKIO_FILESYSTEM_MODE` | invariant | únicamente `ephemeral` fuera del adaptador local |
| `XANTALAL_ROOT` | optional, local_only | conector local de gobernanza |
| `KNOWLEDGE_DISCOVERY_ROOT` | optional, local_only | descubrimiento local explícito |
| `GOOGLE_CLIENT_ID` | required | capacidad Google OAuth |
| `GOOGLE_CLIENT_SECRET` | required, secret | capacidad Google OAuth |
| `GOOGLE_REDIRECT_URI` | required | capacidad Google OAuth |
| `FIREBASE_PROJECT_ID` | required | capacidad Firebase |
| `FIREBASE_CLIENT_EMAIL` | optional, secret | credencial individual Firebase |
| `FIREBASE_PRIVATE_KEY` | optional, secret | credencial individual Firebase |
| `GOOGLE_APPLICATION_CREDENTIALS` | optional, local_only, secret | compatibilidad Cliente Cero |
| `GOOGLE_CLOUD_PROJECT` | optional | credenciales por defecto |
| `OXKIO_ADMIN_FIREBASE_UIDS` | required, secret | autorización ejecutiva |
| `OXKIO_ADMIN_FIREBASE_EMAILS` | optional, secret | allowlist auxiliar |
| `OPENAI_API_KEY` | optional, local_only, secret | simulador, fuera del runtime oficial |

La obligatoriedad se valida al activar cada capacidad, para conservar el arranque local sin convertir integraciones opcionales en requisitos globales. Los diagnósticos solo contienen nombres de variables.

## Inventario de stores y filesystem

| Store o acceso | Clasificación | Tratamiento |
|---|---|---|
| `backend/core/approvalQueue.json` | productivo local, runtime, migración futura | `ApprovalRepository`; no migrado |
| `backend/memory/memory.json` | productivo local, runtime, migración futura | `MemoryRepository`; no migrado |
| `backend/core/executionLog.json` | runtime, auditoría, migración futura | `OperationRepository`/`AuditRepository`; no tocar datos |
| stores de `strategicMemory`, `executiveAgenda`, `projectRegistry` y `securityInventory` | productivo local, runtime, migración futura | inventariados; extracción posterior |
| `backend/knowledge/knowledgeStore.json` y `backend/data/knowledge-store/objects/*.json` | productivo local, runtime, migración futura | metadatos/objetos; extracción posterior |
| estado de `knowledge-change-detector` | runtime, migración futura | estado durable pendiente |
| `backend/auth/googleTokens.json` | secreto, productivo local, migración futura | no leído ni modificado; sacar del filesystem antes de desplegar |
| `approvalQueue.example.json`, `memory.example.json` | ejemplo | mantener fuera de persistencia productiva |
| JSON creados en temporales por `*.test.js` | test fixture | mantener aislados |
| stores de `simulador-ia` | runtime local/laboratorio | excluir de imagen y runtime oficial |
| XANTALAL y `orchestration/*.md` | gobernanza | fuente canónica Git; no es store operativo |
| lectura de estáticos `app/` | artefacto de aplicación | permitida readonly dentro de imagen |
| conectores de carpetas locales/OneDrive | local_only | requieren ruta explícita; no dependencia de arranque |

## Contrato neutral de idempotencia

- `idempotencyKey` estable por intención autorizada y efecto externo.
- Estados: `reserved`, `executing`, `succeeded`, `failed_retryable`, `failed_terminal`, `external_effect_unknown`.
- La reserva y cada transición futura deberán ser atómicas.
- `succeeded`, `failed_terminal` y `external_effect_unknown` son terminales.
- `failed_retryable` permite volver a `executing` solo bajo política explícita.
- Un efecto externo incierto nunca se reintenta automáticamente.
- El resultado externo persistido es mínimo y sanitizado.
- La clave incorpora sujeto/tenant y no contiene secretos.

## Alternativa secundaria candidata

Railway permite alojar con poca fricción el contenedor Node, cron y workers. Mantendría Firebase Authentication y Firestore para evitar una doble migración. Es secundaria por añadir proveedor, coste de servicio continuo e integración menos directa con IAM/Firestore. Docker, repositorios y exportaciones deben mantener la salida hacia Cloud Run.

## Comparativa de runtimes

| Opción | Node/procesos | Jobs/WebSockets | Operación/coste | Decisión |
|---|---|---|---|---|
| Cloud Run | Alta tras `PORT`; disco efímero | Jobs, Scheduler/Tasks; WebSockets | Gestionado, revisiones, pago por uso | Candidata principal pendiente |
| Firebase App Hosting | Media; más orientado a frameworks web; usa Cloud Run | No sustituye workers | GitHub/CDN; Blaze obligatorio | Frontend futuro |
| Firebase Functions | Handlers stateless | Eventos/programación | Gestionado, más acoplamiento | Solo funciones puntuales |
| Railway | Alta; volúmenes/servicios activos | Cron, workers, colas, WebSockets | Desde $5/mes + uso | Alternativa secundaria |
| Render | Alta; disco efímero por defecto | Workers, cron, WebSockets | Simple; precio según plan | Alternativa terciaria |
| VPS gestionado/propio | Alta y persistente | Libertad total | Parches, HA, backups y TLS propios | No ahora |
| LucusHost | unknown | unknown | unknown | Auditar plan |
| Netlify | Frontend/functions | No asumir runtime/WebSocket persistente | Adecuado para frontend | No runtime principal |

Fuentes oficiales: [Cloud Run](https://docs.cloud.google.com/run/docs/overview/what-is-cloud-run), [precios Cloud Run](https://cloud.google.com/run/pricing), [App Hosting](https://firebase.google.com/docs/app-hosting), [costes App Hosting](https://firebase.google.com/docs/app-hosting/costs), [Railway](https://docs.railway.com/pricing/plans), [workers Railway](https://docs.railway.com/guides/cron-workers-queues), [workers Render](https://render.com/docs/background-workers) y [discos Render](https://render.com/docs/disks).

## Comparativa de BBDD

| Opción | Evaluación | Decisión |
|---|---|---|
| Firestore | Documental, serverless, transacciones e integración Auth; exige índices y modelado | Candidata pendiente de auditoría |
| PostgreSQL gestionado | ACID, SQL, constraints, reporting y portabilidad | Candidata futura |
| Cloud SQL PostgreSQL | PostgreSQL gestionado, HA/backups; coste continuo | No añadir todavía |
| Supabase Postgres | PostgreSQL + RLS; proveedor adicional; backups Pro | Alternativa futura |
| MySQL/MariaDB LucusHost | Capacidades, TLS, backup y exportación unknown | No seleccionar |
| SQLite | Archivo único y contención multiinstancia | Solo laboratorio |

No crear ni migrar BBDD durante 5C.7B.1. El primer informe Antigravity está revisado, pero Firestore no se decidirá definitivamente hasta obtener evidencia del piloto y revisar la segunda auditoría externa. PostgreSQL requerirá ADR y migración cuando relaciones, reporting, constraints o volumen lo justifiquen.

## Mapa de datos

| Dato | Fuente canónica/propietario | Lectores/escritores | Sensibilidad | Retención, copia y restauración | Actual → objetivo |
|---|---|---|---|---|---|
| Identidad | Firebase Auth/usuario | backend/Firebase | alta | política Firebase | Firebase → Firebase |
| Perfil/tenant | Firestore/tenant | backend/admin | alta | backup y restore | inexistente → Firestore |
| Memoria | Firestore/usuario | Brain/Memory | alta | configurable; export | JSON → Firestore |
| Tareas | Firestore/tenant | Dashboard/servicio | media/alta | backup diario | Firestore → Firestore |
| Agenda | Calendar/usuario | Calendar service | alta | proveedor + export | Calendar → Calendar |
| Documentos | Drive/Storage/usuario | Knowledge/importadores | alta | versionado + copia | local/Drive → Drive/Storage |
| Metadatos | Firestore/tenant | Knowledge/pipeline | media | backup diario | local → Firestore |
| Operaciones | Firestore/tenant | Supervisor/Coordinator | alta | retención + export | memoria/JSON → Firestore |
| Aprobaciones | Firestore/tenant | Dashboard/Queue | crítica | transacción, backup, restore | JSON → Firestore |
| Auditorías | Firestore + export/XANTALAL | auditor/servicios | crítica | retención por definir | JSON → Firestore/export |
| Logs | Cloud Logging/XANTALAL | operación/runtime | media | 30 días iniciales | consola/JSON → Logging |
| Conocimiento | Firestore + Storage/tenant | Knowledge/Pipeline | variable | versionado/export | filesystem → nube |
| Tokens | store cifrado/usuario | OAuth service | crítica | hasta revocación | JSON → cifrado |
| Config no secreta | GitHub/XANTALAL | runtime/CI | baja/media | Git | JS → entorno |
| Secretos | Secret Manager/XANTALAL | identidad de servicio | crítica | versiones/rotación | fichero/entorno → SM |
| Gobernanza | GitHub/XANTALAL | proyecto/humano | interna | Git + copia Drive | GitHub → GitHub |

## Costes conocidos y desconocidos

- Cloud Run factura CPU, memoria y solicitudes, con nivel gratuito; una instancia mínima cambia el coste.
- App Hosting exige Blaze. Publica cuotas sin coste de 180.000 vCPU-s, 360.000 GiB-s y 2 millones de solicitudes Cloud Run, más límites propios.
- Firestore publica 1 GiB, 50.000 lecturas/día, 20.000 escrituras/día y 20.000 borrados/día sin coste; backup/PITR/restore requieren facturación.
- Railway: Free $0 con $1 de recursos; Hobby $5; Pro $20. RAM $10/GB-mes, CPU $20/vCPU-mes, egress $0,05/GB y volumen $0,15/GB-mes.
- Supabase: Free $0; Pro desde $25/mes; PITR desde aproximadamente $100/mes.
- Render, VPS y LucusHost: unknown hasta fijar plan, región, SLA y recursos.
- Costes para 10, 100 y 1.000 usuarios: unknown. Se requieren solicitudes por usuario, latencia, CPU, respuestas, almacenamiento, lecturas/escrituras, egress, jobs y `minInstances`. 5C.7B.7 debe medir Cliente Cero y producir el modelo; no se inventan importes.

## Riesgos

1. Token OAuth real local: crítico; revocar/rotar.
2. JSON local incompatible con múltiples instancias y transacciones.
3. Rutas legacy amplían superficie de ataque.
4. Backend, estáticos y laboratorio mezclados.
5. Falta aislamiento de tenant probado.
6. Copias sin restauración demostrada.
7. Coste desconocido sin telemetría.
8. Dependencia Google; mitigar con contenedores, repositorios y exportaciones, no duplicando BBDD.
9. Background sin cola durable.
10. LucusHost no evaluable todavía.

## Plan de implementación

| Subfase | Objetivo/archivos | Riesgos y pruebas | Cierre/rollback/autoridad |
|---|---|---|---|
| 5C.7B.1 | Preparación cloud-ready neutral | contratos, suite y contenedor local | artefacto reproducible, sin migración ni proveedor activado |
| 5C.7B.2 | Comparativa de adaptadores y decisión de persistencia | concurrencia, idempotencia, tenant y métricas equivalentes | ADR humana Firestore/PostgreSQL; sin migración anticipada |
| 5C.7B.3 | Secret Manager, IAM, tokens cifrados y rotación | fuga/pérdida de acceso | ningún secreto local; aprobación obligatoria |
| 5C.7B.4 | Staging Cloud Run y pipeline | coste/config; smoke/rollback | staging estable; autorizar Blaze/despliegue |
| 5C.7B.5 | Logs, métricas, alertas y presupuesto | privacidad; redacción/alertas | observabilidad verificada |
| 5C.7B.6 | Backup, export, restore y DR | RPO/RTO; simulacro | restauración demostrada |
| 5C.7B.7 | Piloto Cliente Cero remoto | OAuth, latencia, duplicados | 24/7 observado; draft único; cero envíos |
| 5C.7B.8 | Auditoría Antigravity | hallazgos | sin `CORREGIR`; decisión humana |
| 5C.7B.9 | Probadores | aislamiento/soporte | tenant isolation, consentimiento y rollback |

## Preguntas para LucusHost

1. Plan contratado, renovación y coste total con IVA/excesos.
2. Node 24/7, versión, `PORT`, Docker, SSH, systemd/PM2 y auto-restart.
3. CPU, RAM, disco, IOPS, tráfico, concurrencia y procesos.
4. Persistencia entre despliegues y almacenamiento de objetos.
5. Secretos, rotación, IAM y logs.
6. Cron, workers, colas, WebSockets y límites.
7. BBDD, versiones, conexiones, TLS, backups, PITR y exportación.
8. Regiones, SLA, monitorización, alertas, rollback y soporte.
9. Salida de código, datos, dominios y certificados.
10. DPA/RGPD, ubicación y subprocesadores.

## Puntos de auditoría Antigravity

### Auditoría 1 — selección arquitectónica

Completada antes de seleccionar runtime y persistencia. Veredicto: **APROBADA CON CORRECCIONES**. Sus riesgos válidos quedan incorporados y sus elecciones tecnológicas no son obligatorias. No bloquea 5C.7B.1.

### Auditoría 2 — aceptación operativa

Ejecutar al cierre de 5C.7B.7 y antes de probadores. Prerrequisitos: piloto remoto, tokens rotados, persistencia transaccional, tenant probado, observabilidad, restore, SAFE_DRAFT_ONLY y costes medidos.

## Alcance cerrado de 5C.7B.1

Incluye exclusivamente:

1. `PORT` configurable.
2. Health y readiness.
3. Shutdown controlado.
4. Eliminación de rutas absolutas.
5. Contrato de variables de entorno.
6. Filesystem tratado como efímero.
7. Contenedor reproducible y neutral.
8. Inventario de stores que deberán extraerse.

Excluye:

- migración de datos o stores;
- selección definitiva de runtime o BBDD;
- activación de Cloud Run, Blaze, Firestore nuevo, Secret Manager o facturación;
- contratación de servicios;
- despliegue;
- modificación de SAFE_DRAFT_ONLY o flags de ejecución.

## Próxima acción exacta

Realizar staging selectivo de 5C.7B.1, excluyendo stores, secretos y trabajo ajeno. Después, abrir documentalmente 5C.7B.2 para comparar adaptadores y decidir persistencia con evidencia, sin implementarla todavía.
